# PROJECT STATE — Skydive City

> **Last updated:** 2026-09-03 by PM Agent (**Winterfest additions LIVE (lodging · fleet · vibrant palms, `8cca318`; Casa withheld behind a build-strip flag) · August ops report COMPLETE but HELD for Rich's Burble count (`#41`, decide ~09-04) · Tier 1 APPROVED, §7 still open (`#34`) · T2/T3 estimate delivered ~155–265h (`#43`) · stale-content issue filed with rates-dropped decision (`#44`). Two self-corrections logged: a wrong 'duplicate FAQ pages' finding, and fleet copy inherited from an outdated facilities page.** — see Session Log 2026-09-02/03 top row.) Prior 2026-09-01:  Prior 2026-08-27: **`#40` Cassie event updates SHIPPED to prod — Winterfest/Flockfest/Flamingofest content + graphics live, 5-phase clean (`ebac427`, `a0ea18d`); 2 follow-ups on Live Watch (Burble reg URLs by Sept 1 · Winterfest image pending Cassie). Branch-state drift corrected: `develop` was level with origin, not 5 ahead.** — see Session Log 2026-08-27 top row.) Prior 2026-08-26: **Site Redesign SOW `v1.0-DRAFT` complete (16-sec + actual-AI LOE ~75–125h + milestone timeline; §1/§7 TBD; §10 Client-owns-Compass) + visual companion built + Cassie event-updates request `#40` queued for 2026-08-27** — see Session Log 2026-08-26 top row. 5 SOW commits unpushed; browser screenshot/JS/click tools still broken.) Prior 2026-08-24: **SOW `v1.0-DRAFT` first drafted (`d472f66`).** Prior 2026-08-20 (later): **Burble Manifest embed — dev mockup built for Rich; root-caused as a Burble-side cookie blocker (`#39`).** Prior 2026-08-20: **`/wayfinder` map `#27` charted for the redesign — 6/11 decisions resolved (deploy/host/SEO clusters done); BM session resolves the rest** — see Session Log 2026-08-20 + 08-17. Prior: 2026-08-14 (Site Redesign unblocked + `#24` triaged); 2026-08-06 (July Ops Report sent `#26`; Matt Pocock v1.1 + `/wayfinder` adopted, ADR-0009); 2026-08-05 (BM SDLC reply sent · Claude account consolidated); 2026-07-27 (FFP merch disclaimer + Phase B verified live); 2026-07-22 (**Compass feedback R1 + design-system name FINALIZED → "Compass"** — business feedback applied to the design system + FFP page (crisp meter bands · Bold 92% veil · progress-reactive thumb · scale 25/50/100/250/400/500+ · "Paid" card removed · matrix tier-name colors · FFP reorder Ladder→Worth→Counter · backbone removed · copy) and pushed (`fc23843`); then **Gyro→Compass** rename across the code repo + `compass-mockup/` dirs (`e049cc5`); signoff Artifacts refreshed (`8c59c060`, `ead0c4a0`). See the ⚡ RESUME block. Prior context: **2nd redesign showcase — **Winterfest event landing page built on Gyro** (Artifact `1d5fa51c`, `8a8bae6`; countdown Dec 30 2026 8 AM ET); **FFP footer matched live** (`ead0c4a0` refreshed, `8a5575c`); **BM stakeholder roster corrected** in [[stakeholders]] (James La Barrie=CEO/SDLC-decision · Laura Jane Happick=Dir.Web-Maint · Marcella Smith=GM/analytics). Both repos pushed. See the ⚡ RESUME block.** Prior context: **Design System → showcase Artifact **"Altitude"** (`8c59c060`, label `altitude-v6`) is the client-signoff deliverable; build phase complete + published (DesignSync/Stitch), then HEAVILY REFINED over many James review rounds — mock-up-parity components + real-events View Transitions, single live jump-counter with Paid/Saved/Avg, a fixed cubicBezier token bug, Aurora status colors + mode-aware notices. **`/design-sync` DONE** + **system renamed → "Gyro"** (provisional; content-swept, Artifact same URL); both repos pushed to origin. cd-prompt friction fixed via a block-cd hook. **NEXT (James's call): build the FFP landing-page mock-up ON the Gyro system** — see the ⚡ START-HERE block. (Later: WP-theme port at redesign-SOW time; finalize Gyro filenames/external names once the name locks.)** Prior context: framing grill COMPLETE (Q1–Q7) + ADR-0004. Task #8 (FFP redesign test-bed) grew into authoring the redesign design system: built the FFP showcase on the system (Artifact), then `/grill-with-docs` on the system itself → locked dual-mode (audience-split light/dark) · 3-tier git-canonical semantic tokens (Stitch/DesignSync downstream) · tier palette · IBM Plex Mono · rare-gradient rule · hybrid semantic tokens · motion+View-Transitions. Ratified via ADR-0004; `redesign-discovery.md` reconciled; record in `design-system-discovery.md` + `design-system/visual-aids/`. Housekeeping also cleared: `gh auth switch --user itsginfo` fixed push/`gh`, 51 commits pushed, FFP retroactive issue `#25` filed. Prior context: **🆕 Frequent Flyer Program (FFP) — Phase A SHIPPED to prod (soft launch).** New Project Work from Rich: loyalty/rewards landing page + homepage teaser. `/grill-with-docs` scoped (July-23 deadline, standalone Project SOW, good-faith auth, Rich signed off). Built dev-first: bespoke self-contained dark landing page as a WP page template `template-ffp.php` deployed via single-file `scp -O` (deploy.sh frozen), `/ffp` redirect + theme-CSS-dequeue via 2 mu-plugins, dark site header/footer (ZHILLS logos + real menus), homepage teaser via idempotent ACF migration. **Phase A LIVE 2026-07-20** at `www.skydivecity.com/frequent-flyer-program/` (page + `/ffp` redirect; homepage teaser held for **Phase B ~Aug 1** public launch). Full record: `project_management/frequent-flyer-program/` (`ffp-discovery.md`, `deploy-runbook.md`). git push/`gh` **RESOLVED 2026-07-21** (`gh auth switch --user itsginfo`; 51 commits pushed to `origin/develop`; FFP retroactive issue `#25` filed). Prior context: **Site Redesign `/grill-with-docs` discovery COMPLETE** — Q0–Q8 resolved; canonical name "Site Redesign" ("Phase 2" retired); Option A new WP theme headless-ready (ADR-0003, first product ADR); Tandem>AFF>Events hierarchy; AFF+Events instrumentation prerequisite; full-site-phased with Winterfest `#21` as tracer-bullet pilot; ownership lanes locked incl. Cassie Young=content. Next `/to-prd`→`/to-issues`. 2 external homework items → Rich/BM (PRD assumptions). Prior context: **Weekly-maintenance routine `#23` OPERATIONAL** — plugin updates + contact-form QA, agent-assisted each Wed/Thu. First run complete: all plugins current (Yoast 28.0 done), contact-form test PASS (qa@ received). Verify manual; test entries retained for audit. Exec-summary follow-up email drafted for June chain — James to send. No open setup items; no incidents.)

---

## ⚡ RESUME INSTRUCTION

**Managed Services active. M1 landed today — the client GitHub org exists, the theme repo is seeded, and the deploy pipeline runs green in dry-run.** August ops report is finished and cleared to send.

**Next — the LIVE deploy run (dry-run is already done, twice, both green).** In `Skydive-City/skydivecity-theme`: Actions → *Deploy theme to production* → **`dry_run=false`**. The only queued content change is `page-templates/template-manifest.php` (the `#39` dev mockup — harmless on prod, no page uses it there); **0 deletions**. The workflow smoke-tests `/`, `/winterfest/`, `/frequent-flyer-program/`, `/events/` and fails on a non-200 or PHP error. On success: close **`#3`** and **retire** `deploy.sh` rather than unfreezing it. ⚠️ A `connection unexpectedly closed … code 12` is the known Flywheel transient — **retry once** before diagnosing.

**Also ready:** **`#41`** August report is FINAL and cleared to send (Rich's numbers landed: 489 net → **73% online capture**, level with July) — To Rich / CC Matt. **Winterfest page-update email** drafted (`926e688`) — **flush the Flywheel cache before sending**. **`#44`** stale content unblocked (drop published rates; keep $5 tent, RV varies by slot). **`#42`** awaits Cassie: event schedule + 4–5 *worst-case* LO photos (artifact `20251e9a`).

**Two decisions parked for James.** (1) **Matt is still not an org owner** — the only owners are Rich and `itsginfo`, so the client's sole backup owner is their vendor. (2) **`sc-clean-excerpts.php`** is tracked in our repo but deliberately kept OUT of the client deploy repo — shipping it would change live Events Calendar excerpt rendering.

**Branch check first.** Project `develop` @ `87631c6` + harness `main` — both pushed + clean. **New third repo: `Skydive-City/skydivecity-theme` @ `6d7b6b7`** (client-owned; working copy at `scratchpad/sdc-theme`, recreate from `files/wp-content` if lost). ✅ Design system = **Compass**. ⚠️ **LIVE on prod:** Compass FFP + Winterfest landing (6 sections, palms, Casa) + `/compass` + FFP teaser/Rewards-nav + theme v3.0.3. ⚠️ **Flywheel:** SSH needs `-tt` for `wp`/`ls` (not rsync), prod SQL needs prefix `wp_vu3scguxbw_`, transient failures warrant one retry — all in [[flywheel]]. ⚠️ **Browser MCP tools BROKEN** (7+ sessions) — headless Chrome is the substitute. Read [[stakeholders]] before any outward email. *(absolute paths, never `cd`; `gh auth switch --user itsginfo`; `gh project item-list` needs `--limit`.)*

---

## Wiki Quick-Index

> **Before starting work on a topic below, read the matching page first.** Located at `projects/skydivecity/wiki/[name].md` in this harness. Lightweight stand-in for Phase C's formal query op (deferred at [`ai-agent-harness#3`](https://github.com/itsginfo/ai-agent-harness/issues/3)).

| When working on… | Read first |
|---|---|
| Flywheel hosting, SSH, `deploy.sh`, SSL renewal, `scp -O` quirk | [`flywheel.md`](../../projects/skydivecity/wiki/flywheel.md) |
| Adding/updating WordPress pages or content, ACF Flexible Content, `mywp` theme | [`wp-acf-rendering.md`](../../projects/skydivecity/wiki/wp-acf-rendering.md) |
| Burble booking funnel (`bookings.burblesoft.com`), CSS/copy/analytics customization | [`burble-integration.md`](../../projects/skydivecity/wiki/burble-integration.md) |
| **Any** prod DB write (postmeta, taxonomy, content, ACF) — 5-phase pattern | [`prod-write-procedure.md`](../../projects/skydivecity/wiki/prod-write-procedure.md) |
| GTM / GA4 / Google Ads — IDs, triggers, conversion measurement | [`tracking-stack.md`](../../projects/skydivecity/wiki/tracking-stack.md) |
| Designing claude.ai scheduled remote agents (routines) | [`sandbox-allowlist.md`](../../projects/skydivecity/wiki/sandbox-allowlist.md) |
| **Any outward-facing email / recipient choice / who-owns-what** (client, Beyond Marketing, Omnyra) | [`stakeholders.md`](../../projects/skydivecity/wiki/stakeholders.md) |

See [`wiki/README.md`](../../projects/skydivecity/wiki/README.md) for conventions and [`wiki/sources.md`](../../projects/skydivecity/wiki/sources.md) for the cited-artifacts log.

---

## Project Overview

| Field | Value |
|-------|-------|
| **Project Name** | Skydive City — Phase 1: skydive.city → skydivecity.com |
| **Overall Status** | 🟢 Managed Services Active — SOW v1.1 + MNDA v1.0 Executed 2026-05-12 |
| **Lead Agent** | PM Agent |
| **Human Owner** | James Meirowsky (ITSG Managing Partner) |
| **Primary SPOC** | Rich Muscolino — rich@skydivecity.com (Primary approver as of 2026-04-23) |
| **Secondary SPOC** | Matt Adamson (DZM) — Secondary approver |
| **Start Date** | 2026-04-01 (SOW effective) |
| **Cutover Date** | 2026-04-27 (released 9:00 AM ET) |
| **Phase 1 Accepted** | 2026-05-05 (Rich Muscolino, written acceptance) |
| **Managed Services SOW v1.1 Executed** | 2026-05-12 (countersigned by Skydive City) |
| **MNDA v1.0 Executed** | 2026-05-12 (Effective Date back-dated to 2026-04-01) |
| **Current Mode** | Managed Services — Routine Requests + Sev 1-3 incidents under executed SOW v1.1 |

---

## Links

| Resource | Link / Path | Notes |
|----------|-------------|-------|
| **GH Project #1** (Skydive City Engagement) | https://github.com/users/itsginfo/projects/1 | Cross-repo board view across `skydivecity-com` + `ai-agent-harness`. **PM agent's primary surface.** |
| **Monday Archive** | `skydivecity-com/project_management/monday-archive/` | All 89 Phase 1 Monday items as Markdown (frontmatter + body + update threads). Permanent home for closed items. Subscription cancelled 2026-05-18. |
| **ADR-0001** (issue distribution) | `skydivecity-com/docs/adr/0001-issue-distribution-shape.md` | Per-repo issues + account-level GH Project v2 layered on top. |
| **ADR-0002** (Monday archive policy) | `skydivecity-com/docs/adr/0002-closed-monday-items-archived-not-ported.md` | Markdown archive, no port. Future port-from-archive remains viable. |
| **Project Root (local)** | `/Users/jamesmeirowsky/Projects/SkydiveCity.com` | Local git repository |
| **Project GitHub Repo** | [itsginfo/skydivecity-com](https://github.com/itsginfo/skydivecity-com) | `develop` branch |
| **Harness GitHub Repo** | [itsginfo/ai-agent-harness](https://github.com/itsginfo/ai-agent-harness) | Private repo — `main` branch |
| **Harness Path (local)** | `/Users/jamesmeirowsky/Projects/agent-driven-enterprise` | AI Agent Harness root |
| **Phase 1 SOW** | `project_management/IT Strategy Group _ Skydive City SOW 1.0.md` | Signed Phase 1 SOW — scope, compensation, acceptance criteria. Closed Done 2026-05-05. |
| **Managed Services SOW v1.0** | `project_management/IT Strategy Group _ Skydive City Managed Services SOW 1.0.md` (+ `.pdf`) | Successor instrument to Phase 1 SOW. v1.0 issued 2026-05-07. **Superseded by v1.1** 2026-05-09; preserved alongside v1.1 for historical record. |
| **Managed Services SOW v1.1 — Executed** | `project_management/IT Strategy Group _ Skydive City Managed Services SOW 1.1 - Executed.pdf` | **EXECUTED 2026-05-12** (countersigned by Skydive City; reported by James 2026-05-14). Active governing instrument. §11 carries the five enumerated Confidential Information categories. |
| **MNDA v1.0 — Executed** | `project_management/IT Strategy Group _ Skydive City MNDA 1.0 - Executed.pdf` | **EXECUTED 2026-05-12** (countersigned by Skydive City). Standalone mutual instrument now in force. Effective Date back-dated to 2026-04-01 retroactively covers any Confidential Information disclosed under the Phase 1 SOW. |
| **CONTEXT.md** | `CONTEXT.md` (skydivecity-com repo root) | Canonical glossary for the engagement: Managed Services, Routine Request, Project Work, Project SOW, Phase 1. Single-context layout per `docs/agents/domain.md`. |
| **Phase 1 Project Plan** | `project_management/Phase 1 Project Plan.md` | Full WBS, timeline, risks |
| **Release Runbook** | `project_management/release-night-runbook.md` | Used in 2026-04-27 cutover |
| **Post-Deployment QA Plan** | `project_management/post-deployment-qa-plan.md` | Browser QA scenarios for Antigravity |
| **Retrospectives folder** | `projects/skydivecity/retrospectives/` (in harness) | Weekly autonomous retros (routine currently disabled). First retro: `2026-04-27.md`. |
| **Project Wiki** | `projects/skydivecity/wiki/` (in harness) | Compounding knowledge surviving across sessions. **Phase B live (2026-05-18):** 6 entity pages — see Wiki Quick-Index above. **Phase C deferred** at [`ai-agent-harness#3`](https://github.com/itsginfo/ai-agent-harness/issues/3). |
| **Harness-level Wiki** | `wiki/` (harness root) | Cross-cutting harness knowledge. |
| **GA4 Pre-Release Baseline** | `project_management/ga-baseline/README.md` | 7-day traffic baseline pre-cutover (R-3 input for Completion Report). |
| **Booking Conversion Scorecard** | `project_management/booking-conversion-scorecard/README.md` | v0 skeleton built during the #9 investigation. 2026-05-18 onward is the clean baseline (funnel was dark before). |
| **Deploy Script** | `deploy.sh` (project root) | rsync themes/plugins to Flywheel. **FROZEN** pending `skydivecity-com#3` investigation. |
| **Import Script (used in cutover)** | `migration/wp-import.sh` | The canonical import script. |
| **CTA Import Script** | `migration/wp-cta-import.php` | Sets `sc_register_url` + `sc_facebook_url` postmeta on events. |
| **Image Import Script** | `migration/wp-image-import-local.php` | Replaces broken `wp-image-import.php` (skydive.city URLs are dead). |
| **Technical Backlog** | `project_management/technical_backlog.md` | Cross-cutting tech tasks outside Phase 1 scope. |
| **GA4/GTM Audit (W1-10)** | `project_management/W1-10-tracking-audit.md` | GA4/GTM tracking audit ✅. |

---

## Current Sprint Context

**Mode:** Managed Services — active under executed SOW v1.1 + MNDA v1.0 (both 2026-05-12).
**Posture:** Handle Routine Requests + Sev 1-3 incidents as they arise; work polish backlog (`skydivecity-com#1`–`#4`) opportunistically; gather scope for SCOPE-1 (Burble booking calendar) when Rich/Matt are available; no committed Phase 2 cadence.

> For task list and statuses, see [GH Project #1](https://github.com/users/itsginfo/projects/1). Below is context the tracker doesn't capture.

### Operating Notes
- **Site is live and stable.** Phase 1 monitoring window closed 2026-05-04 at 100.000% / 168h. UptimeRobot remains active across 4 monitors.
- **Polish backlog** lives at `skydivecity-com#1`–`#4` (all `routine-request`): Burble + FB CTAs, featured images, `deploy.sh` 17K-file delta, SSL renewal verification.
- **Change-control standard for prod DB writes:** 5-phase procedure per [`wiki/prod-write-procedure.md`](../../projects/skydivecity/wiki/prod-write-procedure.md) — read-only inventory → SHA-verified upload → execute with logged output → live verification → checkpoint.
- **`deploy.sh --live` remains FROZEN** pending `skydivecity-com#3` investigation. Independent surface from prod DB writes — they never run together.
- **Daily monitoring routine remains disabled** (sandbox allowlist limitation; see [`wiki/sandbox-allowlist.md`](../../projects/skydivecity/wiki/sandbox-allowlist.md)).

---

## Live Watch

> Time-sensitive standing items with known expirations. Items leave this table when the date passes or the condition resolves. Per V-003 triage taxonomy — other standing facts live in `CLAUDE.md` (project facts), `docs/adr/` (architectural rules), or `wiki/*.md` (stable systems knowledge).

| Item | Watch by | Tracker | Notes |
|------|----------|---------|------|
| **Burble registration link — Flamingofest 2027** | **2026-10-01** (registration opens) | [`#40`](https://github.com/itsginfo/skydivecity-com/issues/40) | Same shape as the Flockfest row, one month later — **note the different date**; `ticket_website` empty on **5867**. Its $99 early-bird runs Oct 1 → Oct 31, so the link needs to be live on day one or the early-bird window is dead on arrival. |
| **August Digital Ops Report — HELD for Rich's Burble count** | decide by **~2026-09-04** (SOW §4.1 window edge is ~09-05–09-08; Labor Day 09-07 shifts it) | [`#41`](https://github.com/itsginfo/skydivecity-com/issues/41) | **Report is COMPLETE and QA'd** (`049ead5`) — Sections 1–6 filled, PDF rendered (5pp A4, poppler-checked), delivery email drafted. **James's call 2026-09-02: send only once Rich's August Burble numbers arrive.** When they land it is a ~10-min turn: fill Section 4's reconciliation against the **357 online tandem items** already stated in the section (ratio vs July's ~72%, directional), re-render, update the email's third bullet, send (To Rich, CC Matt). **If Rich is quiet by ~09-04**, decide: send as-is and carry the ratio into September, or accept late. A follow-up nudge to Rich is drafted-on-request. |

> *Retired 2026-09-03 (later):* "Casa announcement — Winterfest fleet" row — ✅ **ANNOUNCED + LIVE on prod** (5-phase, `514c848` + `d053d29`). The **embargo gate was removed, not just flipped** — it protected nothing once announced, and a conditional nobody exercises rots (editing the lede immediately leaked "Casa" outside its markers and broke the stripped stat band). Strip mechanism preserved in git history at `8c8ebf3` if another embargo needs it. Stat band now reads **3 + 2 + 1 = 6**, which also retired the unverified 13,500 ft jump-altitude figure.
> *Retired 2026-09-03:* "GitHub account creation meeting with Rich" row — watch date reached. Prep written + narrowed to James's two-item scope (`62659cc`), carrying the do-not-transfer-the-repo finding and the `itsginfo`-account answer. Outcome not yet reported back; if the org was created, the follow-on work (theme repo, dedicated deploy key, Actions→SSH pipeline) is recorded in the prep doc's "ITSG executes after" section.
> *Retired 2026-09-01:* "Burble registration link — Flockfest 2027" row — ✅ **SHIPPED to prod on its open date.** URL arrived from James via Matt (`https://bookings.burblesoft.com/index/53/1561`, verified → Flockfest Vol. 2 calendar, $249 early-bird). 5-phase clean; `ticket_website` set on **5866** + opens-on notice swapped for the register link. Commits `6b1e7fc` + log `…-prod-20260901T155439Z.log`. **Flamingofest 2026-10-01 row below is still live.**
> *Retired 2026-08-27:* "Winterfest featured image — Cassie's call" row — ✅ RESOLVED same session. Cassie confirmed the swap for **both** surfaces she named; applied to the Events Calendar thumb (5865 → attachment 5909) *and* the landing-page hero key art (rebuilt into `template-winterfest.php`). Commits `86fa7d7` + `53b65b1`.
> *Retired 2026-08-26:* "Claude account: gmail → Free" row — watch date 2026-08-22 passed; gmail Max auto-reverted to Free as expected (old artifacts/shared links persist as fallback). No action taken/needed. **Deferred:** reconnect the Gmail MCP connector under itsginfo when mail is next needed.
> *Retired 2026-08-06:* "July Digital Ops Report" row — ✅ FINALIZED + SENT (To Rich, CC Matt; `#26` closed). First clean full-month baselines: **6.8% open-to-purchase**, **~72% online-capture**. Commits `7526363`→`e6fd872`.

> *Retired 2026-07-06:* "June Digital Ops Report — SEND" row — ✅ SENT (To Rich, CC Matt); net-507 Burble count addressed directly per May template. Superseded by the "July Digital Ops Report" row above. Draft `dfdad30`.
> *Retired 2026-07-02 (later 4):* "`#18`/`#17`/`#19`/`#22` — prod push" row — ✅ ALL 4 SHIPPED + live-verified on prod (5-phase, SHA-verified); issues closed. Commits `4337b5b`/`38fb4f0`/`27d1730`.
> *Retired 2026-07-02 (later 3):* "🚧 Flywheel SSH outage" row — ✅ RESOLVED. Single non-looping probe returned `SSH_OK` (rc=0); throttle/outage cleared. Prod-write blocker gone.
> *Retired 2026-06-29 (late):* "#16 Omnyra AFF repoint" row — ✅ EXECUTED on prod (5-phase clean) + CLOSED, commit `a57748e`. 4 fields → `academy.skydivecity.com`, verified live. Careers-page rec carried on the June report row above.
> *Retired 2026-06-29:* "#15 GA4 purchase-tracking incident" row — ✅ CLOSED. BM executed Option B; native events verified firing post-change. Mirror-image model held; event-based reporting confirmed. May-report correction carried on the June report row above.
> *Retired 2026-06-29:* "#16 Omnyra walkthrough" row — walkthrough done 2026-06-22; superseded by the "#16 Omnyra AFF repoint" row above (scope now defined: Routine Request, repoint staged, held on cert).
> *Retired 2026-06-12:* "#9 Google Ads dashboard reconciliation" row — superseded by [`#15`](https://github.com/itsginfo/skydivecity-com/issues/15): Marcella's 2026-06-11 email IS the vendor reply, and the reconciliation question is absorbed into the incident's root-cause finding (Ads conversion tag only began firing 05-18; purchase pipeline silenced same day).
> *Retired 2026-06-02:* "May report — James review + send" row — ✅ delivered 2026-06-02 (To Rich, CC Matt), 3 days ahead of the SOW §4.1 deadline; [`#10`](https://github.com/itsginfo/skydivecity-com/issues/10) closed. June report row added above.
> *Retired 2026-06-01:* "May Burble booking count" row — Rich provided 538 May tandem reservations 2026-06-01; §4 reconciled. Superseded by the review+send row above.
> *Retired 2026-06-01:* "SSL cert renewal" row — verified resolved ([`#4`](https://github.com/itsginfo/skydivecity-com/issues/4) closed). Renewal fired May 8; Google Trust Services wildcard cert valid → 2026-08-06; next auto-renewal ~2026-07-07 needs no action. Issuer fact (was wrongly "Let's Encrypt") corrected in [`wiki/flywheel.md`](../../projects/skydivecity/wiki/flywheel.md).
> *Retired 2026-05-20:* "T+24h GA4 re-pull" row (watch date 2026-05-19 passed; verification action remains as an open checkbox on `skydivecity-com#9` — the issue is the canonical owner now, not this Live Watch table per V-003 rule "items leave this table when the date passes or the condition resolves").

---

## In-Flight Tasks ⚡

> Active work tracked in [GH Project #1](https://github.com/users/itsginfo/projects/1). Below is narrative context the issue body doesn't capture.

- **🆕 Site Redesign (T1) Project SOW (`skydivecity-com#34`) — TIER 1 APPROVED TO PROCEED 2026-09-02; §7 COMPENSATION STILL OPEN.** Rich gave the go on scope; the commercial conversation is unfinished, so the SOW stays `v1.0-DRAFT` with **§1 dates + §7 compensation `[TBD]`** and the PDF still stale on §4.3. **§10 = Client owns all deliverables incl. an explicit Compass assignment.** M1 (GitHub/SDLC setup) is being started ahead of signature — fine as goodwill, but the line to watch is where account setup becomes theme building. **`#43` is the intended re-entry to §7** — it answers Rich's own question and reframes the conversation as information rather than a demand.
- **🆕 Tier 2/3 LOE (`skydivecity-com#43`) — DELIVERED.** `project_management/redesign-t2-t3-loe.md` (`413406f`, corrected `33f83f0`): **~155–265h full site** (T1 75–125 · T2 13–23 · T3 65–115), estimated **per archetype × count** off a verified 56-page prod inventory. Headline argument: T2+T3 cover ~7× the pages of T1 for about the same effort, because T1 buys the system once. Inventory verification also found **13 orphaned pages** — 3 USPA-2020 behind a **404 parent** yet still in the XML sitemap, and **all 10 coach pages unlinked from their own parent** (an IA defect M5 repairs).

- **✅ Cassie event updates (`skydivecity-com#40`, routine-request) — ONE dated follow-up left.** Content, graphics, the Winterfest banner swap and **Flockfest's Burble link (shipped on its 2026-09-01 open date)** are all live. **Remaining: Flamingofest 2026-10-01** — `ticket_website` empty on prod **5867**; same job in the `$flamingofest` heredoc of `migration/wp-event-update-boogies-2026-08.php`, dev-first → 5-phase. **$99 early-bird runs Oct 1–31**, so day-one or the window is dead. Issue closes when it lands.
- **🆕 August Digital Ops Report (`skydivecity-com#41`, routine-request) — COMPLETE, QA'd, deliberately UNSENT.** All six sections filled, PDF rendered (5pp A4, poppler-checked), delivery email drafted and marked HELD. **Blocked only on Rich's August Burble count**; Section 4 pre-states the **357 online tandem items** the ratio computes against, so it is a ~10-min turn on arrival. **Decision date ~2026-09-04** against the SOW §4.1 window (Labor Day 09-07 shifts the edge). Commits `049ead5` → `cd4beb3`.
- **🆕 Winterfest page additions (`skydivecity-com#42`, routine-request) — 3 of 5 SHIPPED to prod 2026-09-03 (`8cca318`).** Live: **Where to stay** (headed paragraphs on cards, two-up, + Firefly reservation CTA), **The fleet** (2 Otters + 2 Cessna 208 Caravans year-round, third Otter for the boogie), **vibrant palm backdrop** at 30% screen blend. Sections now 01–06. **Casa built but WITHHELD** — the build *strips* it rather than commenting it out, so the delivered page carries 0 occurrences; `INCLUDE_CASA=1` + redeploy publishes it and flips the count 5→6. **Remaining: (a) event schedule** — waiting on Cassie; **(b) LO headshots — BLOCKED on design input**, see artifact `20251e9a`: no CSS treatment makes a light-background photo seamless on a dark page, cut-out-as-WebP served from the **Media Library** (never inlined) is the only seamless-and-recognizable route, and the treatment cannot be chosen until 4–5 *worst-case* photos are seen. Also carries a scope question — photos turn the LO card from a name list into a portrait roster, which is what could push `#42` past the ≤8h Routine Request line.

- **🆕 Public Burble Manifest embed (`skydivecity-com#39`, project-work) — dev mockup built; BLOCKED on Burble cookie.** Rich's request: put the public Burble Customer Manifest board on the site; James → inline reveal-on-click UX (not new tab). Dev-only: WP page 5892 `/live-manifest-mockup/` + `template-manifest.php` (theme-wrapper structure, lazy iframe on click, fallback link). **Blocker:** Burble's display session cookie is `Secure; HttpOnly` **without `SameSite`** → cross-site iframes can't carry it → "Login failed" (confirmed HTTP+HTTPS; wiki'd in [[burble-integration]]). **Fix is Burble-side** (`SameSite=None; Secure`). **Next:** (1) draft Burble ask (for Rich to forward); (2) James to verify the dev layout (self-verification blocked — browser screenshot/JS tools broke this session). Commit `d04c9bc` (not pushed). Project Work → SOW paper-trail before any prod path.
- **Weekly maintenance routine (`skydivecity-com#23`, routine-request) — OPERATIONAL. Latest run 2026-08-21 CLEAN** (12 minor/patch patches; smoke PASS; contact-form PASS via qa@; log `maintenance-logs/2026-08-21-run.md`). **⚠️ Held follow-up: Gravity Forms 2.10.5 → 3.0.3 (major 2→3)** — runs the contact form; schedule a deliberate dev-first upgrade + form re-test. **First run COMPLETE 2026-07-08.** Recurring Managed Services scope: weekly plugin updates + contact-form test. D1 agent-assisted in-session · D2 minor/patch auto + hold majors · D3 test-route → **`qa@itsginfo.com`** (conditional GF notification on `WEEKLY-TEST` marker; info@ untouched, gets a copy — James okayed). Runbook `project_management/weekly-maintenance-runbook.md` (OPERATIONAL); log `maintenance-logs/2026-07-08-first-run.md`. **First run:** DB-backed-up → **9 patches applied** (Yoast 27.9→**28.0** HELD) → smoke PASS → **contact-form test PASS (James submitted via browser; qa@ received)**. Verification is **manual** (qa@ receipt); WP Mail Logging stays inactive; **test entries retained for audit** (both James's decisions). Mail fact: WP Mail SMTP (Gmail) forces From → info@skydivecity.com (allowlisted on qa@ group). Commits `9c78d2d`/`d56666c`/`6d0027b`/`1af50d7`. Flywheel WP-CLI path `/www/.wordpress` in [[flywheel]]. **Yoast 28.0 applied by James 2026-07-08** (verified 28.0, 0 pending, smoke clean) → all plugins current. Exec-summary follow-up email (`correspondence/2026-07-08-weekly-maintenance-followup-rich.md`) for the June-report chain — **SENT 2026-07-08.** **Loose end:** confirm standing weekly window (rec Tue/Wed AM ET). Distinct surface from `deploy.sh` (`#3` frozen).
- **✅ GA4 purchase-tracking incident (`skydivecity-com#15`, Sev 3) — CLOSED 2026-06-29.** BM (Marcella) executed **Option B** in her GTM container — removed GTM's GA4 config tag (+ dup FB pixel) so Burble's native gtag pipeline owns `G-FRW7R7G8EC` again; post-change testing confirmed native events firing → purchase/revenue/product/funnel tracking + the GA4-imported "Burble Purchase" Ads conversion restored. Validated the mirror-image model (the 2026-05-18 `#9` fix had swapped which pipeline GA4 listened to) and confirmed BM's reporting is event-based (so B = viable-not-compromise). No Burble change. **Tail:** facts-only May-report window correction (166/$68,298 = May 1–17 pre-fix) folds into the June Ops Report.
- **✅ Omnyra AI integration (`skydivecity-com#16`, Routine Request) — AFF repoint EXECUTED on prod + CLOSED 2026-06-29 (late).** Cert gate cleared (self-verified: `academy.skydivecity.com` valid Let's Encrypt cert, live FJC signup, `/book`+`/portal` 200). 5-phase prod write clean: read-only inventory matched dev (4 fields — 1099 ×2, 1613 nav, 1872 "AFF CTA" block), SHA MATCH, executed `migration/wp-aff-omnyra-academy-repoint-2026-06-29.php` → 4 fields → `https://academy.skydivecity.com`, verified live (0 legacy refs, academy links rendering, HTTP 200, no PHP errors on `/learn-to-skydive/` + `/faqs/`). Per-event forms untouched. Commit `a57748e` (develop, pushed). **Carry-forward:** `careers.skydivecity.com` DNS-live but unwired (no careers section today) → June-report recommendation to create a careers/employment page.
- **Media-package pricing update (`skydivecity-com#13`, routine-request) — ✅ CLOSED 2026-07-06.** Website: all 7 fields updated CLEAN 2026-06-11 (`pricingtable` 5613 ×6 + orphaned `bookinglink` 5508); script `migration/wp-media-pricing-update-2026-06-11.php` (commit `4c242e0`). Burble-side config confirmed by Rich (via James) 2026-07-06 → site + funnel agree → issue closed.
- **Dynamic Pricing Calendar — ON HOLD per Rich 2026-06-11** — proceeding eventually, but NOT with time-of-day variations yet; wants to monitor media-price impact first. Rich updated the shared sheet (notes in red) — pick up there when re-activated. **Carry-along:** when enabling "Display lowest prices available in calendar" + price-per-timeslot, style the slot buttons with price *next to* the time (Rich's preference, ex-`#14`).
- **E-commerce Merch Discovery — Project Work (no tracker item yet; pre-PRD per V-007)** — `/grill-with-docs` scoping toward a PRD + exec-summary-with-LOE. Working notes: `skydivecity-com/project_management/ecommerce-merch-discovery.md` (commit `1e304ac`). **Locked:** Square Online + Square POS, one native ledger, no sync connector, `shop.skydivecity.com` link-out (Q1–Q4); catalog shape + scale, no personalization, staff-owned photos/copy (Q5). **Flagged risks:** (a) no catalog owner designated today → client-side staffing dependency; (b) catalog is dynamic → recurring ops + Option-B lean. ⛔ **BLOCKED** on the Burble-tender question (see Blocked Items). Resume on an unblocked branch (licensing/subscription costs, returns/exchanges, customer accounts, gift cards, reporting) or wait for Burble confirmation.
- **Digital Ops Monthly Report — recurring (SOW §4.1).** May (`#10`) delivered 2026-06-02; **June SENT 2026-07-06** (draft `dfdad30`; net-507 Burble count addressed per May template; `#13`/careers-page carried). Reusable template + procedure at `project_management/reports/digital-ops-monthly/`. **Next cycle: July report ~2026-08-05** — first clean full-month conversion baseline; report GA4-vs-Burble as a ratio not equality (see Live Watch).
- **Polish backlog (3 GH issues open, all `routine-request`)** — `skydivecity-com#1` (Burble + FB CTAs — needs source URLs from Rich/Matt), `#2` (featured images — has source files locally, needs `wp-image-import-local.php` mapping additions), `#3` (deploy.sh 17K-file delta — `deploy.sh --live` is FROZEN until resolved). Closed: `#4` (SSL renewal verification) on 2026-06-01 — auto-renewal verified resolved.
- **Harness-improvement backlog (3 GH issues open, all `harness-improvement`)** — `ai-agent-harness#1` (Proactive Checkpoint Protocol enforcement; recurring), `#3` (Wiki Phase C — deferred-tracker, 0/4 trigger conditions fired; re-evaluate ~2026-08-18; graphify parked here 2026-06-10 as candidate query-op implementation), `#5` (P-002 gaps audit — V-008 ratified this as REVIEW stream-1 work; canonical home for the audit work). Closed in this engagement: `#2` (Wiki Phase B) on 2026-05-18; `#4` (Retro vs REVIEW reconciliation) on 2026-05-19; `#6` (Monday → GH migration) on 2026-05-18; `#8` (tool-landscape v1) on 2026-05-20 with all 10 verdicts + 8 ADRs + crib + 4 stretch artifacts; `#7` (harness `CLAUDE.md` + slash commands tracker-agnostic) on **2026-05-20** (commit `4298e4d`); `#9` (QUICKSTART → ADE onboarding doc + MethodRX tracker drift) + `#10` (Monday-staleness sweep, 21 files) on **2026-06-10** (commits `bc0dce8` + `a65d8b0`).

**Site Redesign — Project Work (discovery COMPLETE 2026-07-13; pre-PRD per V-007):**
- **`/grill-with-docs` discovery COMPLETE (Q0–Q8)** — `project_management/redesign-discovery.md` (commits `b79e9a4`→`b7553f7`). **First product ADR: [ADR-0003](https://github.com/itsginfo/skydivecity-com/blob/develop/docs/adr/0003-redesign-platform-wordpress-theme-not-headless.md)** (platform).
- **Decision spine:** (Q0) named **"Site Redesign"**, one Project SOW, **"Phase 2" retired as a term** (CONTEXT.md conflict); "ecommerce" = **Tandem/AFF/Events sign-up integration**, merch/Square **deferred** (decouples the blocked merch thread). (Q1) conversion hierarchy **Tandem > AFF > Events** (business strategy); Rich's "AFF buried" = **visibility fix, not strategy shift**. (Q2) "5-star" = qualitative CX north star + **3 measurable layers**; **AFF+Events instrumentation = in-scope prerequisite** (only Tandem/Burble is tracked today). (Q3) **full-site end state, phased** (T1 this SOW / T2+T3 sequenced); **design system = first-class T1 deliverable** authored whole-site up front. (Q4) 6 content-model requirements ("editing stops being archaeology"). (Q6) **Option A — new source-controlled WP theme, headless-ready**; headless deferred (measured future swap), full re-platform rejected — ADR-0003. (Q5) **branded link-out** handoff + **"starting at" pricing** (single source). (Q7) T1 phased: **Phase 0 enabling** (git repo + SDLC + BM align + design system + instrumentation/baseline) → **Phase 1 tracer bullet = Winterfest `#21`** → **Phase 2 spine** (homepage → Tandem → AFF → Events). (Q8) lanes: **ITSG**=theme/git, **Cassie Young** (`events@`)=content, **Rich**=pricing, **Beyond Marketing**=tracking; **content-owner risk RESOLVED** (Cassie designated).
- **2 external homework items** (PRD assumptions, NOT blockers): (a) pricing-messaging framing → **Rich**; (b) git-based SDLC adoption → **Rich + Beyond Marketing** contractual check (organizational-workflow implications, positive prior). **(b) IN MOTION 2026-07-13:** BM SDLC-workflow email APPROVED (`correspondence/2026-07-13-beyond-marketing-sdlc-workflow.md`) — to **Laura Jane** (BM Website/Mktg Mgr, per new [[stakeholders]] wiki; NOT Marcella/analytics), framing anchored on **scale/multi-engineer enablement** (grilled: LocalWP already gives BM separation+preview). **SENT to Laura Jane (confirmed by James 2026-07-17). REPLY RECEIVED 2026-08-05** — from **James La Barrie** (BM President; escalated up from Laura Jane), dev **Kevin** cc'd. Substantive scoping pushback, not a rejection: (1) big-picture gap — thought redesign was events-only; (2) their LocalWP→direct-push works; open to evolving; (3) **git**: never done git for client work → ITSG must own setup + docs (they'll follow, can't architect) — aligns with our ITSG=git lane; (4) **cost**: setup + learning-curve time sits outside retainer, higher rate; (5) **review**: second-eyes-on-*every*-change compounds over a year; (6) **the real question**: how much new dev work? overhead only worth it if significant engineering, else friction > benefit — wants the roadmap. **Our answer is in hand** (`redesign-exec-summary-loe.md`: full-site phased new theme, T1 ~120–210 prof-hrs). Response being drafted 2026-08-05; can reframe review as risk-tiered (V-005 gate, not blanket).
- **PRD `#24` triaged** → `ready-for-human`; **`/wayfinder` map `#27` CHARTED (tickets #28–#38, Project #1); 6/11 resolved** (2026-08-20): deploy/host/SEO clusters DONE (#28 cross-domain, #29+#3 deploy diagnosis, #38 GH-Actions→SSH, #33 Flywheel-stays, #31 no-SEO-owner→ITSG-preservation, #36 SEO approach). **Open:** BM session #30 cutover (4 options → ADR)/#35 analytics-boundary/#37 Winterfest-role; Rich #32 pricing (defer to homepage copy)/#34 SOW (gates build, off BM). Visual map Artifact `631f1da6`. **Next:** run the BM working session → resolve #30/#35/#37 → graduate fog → `/to-spec` → `/to-tickets`. exec-summary+LOE DONE (`9949890`).
- **Redesign sales showcases (Compass, business-dev/unbilled):** client-viewable Artifacts to sell the redesign — **FFP page** + **Winterfest event page** + the **Compass showcase**, all on the canonical Compass system, content grounded in live/approved sources; sources in `project_management/frequent-flyer-program/compass-mockup/` + `winterfest-landing/compass-mockup/` + `design-system/showcase/` (rebuild via the `build-*.mjs` scripts). **⚠️ RE-PUBLISHED under the itsginfo Claude account 2026-08-05** (account consolidation) — **current itsginfo URLs:** Compass showcase `f9921e1d` (publicly shareable) · FFP `d87839ba` · Winterfest `69456817`. *(Superseded gmail-account URLs — Compass `ef0fe125`, FFP `ead0c4a0`, Winterfest `1d5fa51c` — still live while gmail stays on Free, but new links = itsginfo.)* **Client-share:** the Compass showcase is publicly shareable; the **FFP + Winterfest pages are NOT** (public-share gate blocks branded real-org pages) → deliver as self-contained HTML attachments (`compass-mockup/ffp.html`, `winterfest.html`). Natural next input if we extend Winterfest: a **day-by-day schedule from Rich**. `#21` remains `needs-info` (showcase ≠ production pilot).

---

## Pending Project SOWs

> Scope identified but not yet signed. Each becomes a Project SOW per the 2-prong SOW model (Managed Services + per-Project) ratified 2026-05-07. Project SOWs stand separately from the Managed Services SOW — both can be active concurrently per [`CONTEXT.md`](https://github.com/itsginfo/skydivecity-com/blob/develop/CONTEXT.md) glossary.

- **SCOPE-1: Burble booking calendar updates.** New scope from Skydive City: updates to the bookings / booking calendar page hosted on Burble (`burblesoft.com`). **Captured 2026-05-04 from James.** Specific changes TBD — needs detail-gathering session with Rich/Matt. Implementation owner depends on Burble's customization model (third-party SaaS — ITSG may not have direct write access). Out of Managed Services scope; addressed under a Project SOW. Once scope is gathered and signed, the implementation work becomes one or more `skydivecity-com` GH issues. Full Monday history: [`monday-archive/SCOPE-1.md`](https://github.com/itsginfo/skydivecity-com/blob/develop/project_management/monday-archive/SCOPE-1.md).
- **~~SCOPE-2: Omnyra AI integration~~ — RETIRED 2026-06-29; NOT a Project SOW.** Post-walkthrough (2026-06-22), the ITSG-side work turned out to be DNS (done) + repointing 4 WordPress fields — the tool runs entirely on Omnyra's infra. Reclassified as a **Managed Services Routine Request** ([`#16`](https://github.com/itsginfo/skydivecity-com/issues/16)); see In-Flight Tasks. No separate SOW needed.

---

## Blocked Items

> For active blockers, see GH issues with status `Blocked` on [Project #1](https://github.com/users/itsginfo/projects/1).

**Site itself: no blockers** — stable Managed Services posture.

**Public Burble Manifest embed (`#39`, Project Work) — ⛔ blocked on Burble.** The dev mockup is built, but the board can't load in a cross-site iframe: Burble's display session cookie is `Secure; HttpOnly` with **no `SameSite`**, so browsers withhold it from third-party iframes → "Login failed" (confirmed HTTP *and* HTTPS). **Only Burble can unblock** — set the cookie to `SameSite=None; Secure` (ideally `Partitioned`) or provide an embeddable URL/token. **Waiting on:** ITSG to draft the ask → Rich/Burble. A reverse-proxy workaround exists but is fragile + not deployable on Flywheel (rejected). Detail in `#39` + [[burble-integration]].

**E-commerce Merch Discovery (Project Work) — ⛔ inventory branch blocked.** Customers can pay for merch with their **Burble balance** (James, confirmed-pending 2026-05-28), so Burble is likely a third stock-decrementing tender channel. Problem splits into (a) stock decrement — solvable by routing stock to Square; (b) **stored-value tender** — the sticky part, since Burble balance lives in Burble and a Square sale can't natively be paid with it. **Gates the locked "single native Square ledger" architecture** and may raise scope to "consolidate a 3-channel setup" (candidate ADR). **Waiting on James:** confirm with Rich/Matt — does a Burble-balance merch sale decrement stock; what share of sales use it; API/export availability; why merch is in Burble; **crux: is Burble-balance merch tender a hard requirement or negotiable?** Full analysis + options in `ecommerce-merch-discovery.md`.

---

## Open Questions

| # | Question | Status |
|---|----------|--------|
| 1–9 | Phase 1 + Monday-migration questions | ✅ All resolved (see Session Log / ADRs). |
| 10 | **Do the remaining ~23 LO photos share a background type?** Decides the headshot treatment on `#42` — mixed backgrounds force cut-out or monotone; consistent ones allow something lighter. | ⏳ Asked of Cassie in the drafted email (`926e688`). |
| 11 | **Do the on-DZ camping rates stay published?** `#44` — the $5 tent rate is flat and stays; RV varies by slot type, so no single figure can be published. | ✅ Resolved 2026-09-02 (James). |
| 12 | **Was the Skydive City GitHub org created on 2026-09-03, and at what permission level was `itsginfo` added?** Gates the theme repo, deploy key and Actions pipeline. | ⏳ Awaiting James's readout. |

---

## Next 3 Actions (Prioritized)

> When you complete one, update the GH issue first (status, comment if substantive), then return here.

1. **[`#41` — decide by ~2026-09-04] August Digital Ops Report, complete but HELD.** Waiting on Rich's August Burble count. On arrival: fill Section 4's reconciliation against the **357 online tandem items**, re-render + re-QA the PDF, fix the email's third bullet (still reads as though the count is missing), send To Rich / CC Matt. If quiet past ~09-04, decide send-as-is vs late — SOW §4.1's window edge is ~09-05–09-08 (Labor Day 09-07).
2. **[`#42` — blocked on client input] Winterfest schedule + headshots.** Three additions are live. Need from Cassie: the **event schedule**, and **4–5 worst-case LO photos** before any headshot treatment is chosen (artifact `20251e9a` explains why the best photo is the wrong one to design against). Page-update email drafted (`926e688`) — **flush the Flywheel cache before sending**.
3. **[Strategic] Site Redesign — §7 compensation, via `#43`.** Tier 1 is approved to proceed but the SOW is unsigned. Use the delivered T2/T3 estimate (~155–265h end-to-end) to reopen §7 with Rich. **BM working session** still awaiting BM's date → resolves `#30`/`#35`/`#37` → `/to-spec`. Report back the outcome of the 2026-09-03 GitHub/SDLC meeting and, if the org exists, execute the "ITSG executes after" checklist (theme repo, dedicated deploy key, Actions→SSH pipeline — which finally unfreezes `#3`).
4. **[`#44` — unblocked, ready to work] Stale live content.** Scope agreed: **drop published rates** (keep the $5 tent rate; RV varies by slot → point at Firefly). Correct the facilities-page fleet copy from the facts in the issue. Fold in the 3 orphaned USPA-2020 pages from `#43`.
5. **[`#40` — dated 2026-10-01] Flamingofest Burble registration link.** See Live Watch.
6. **[Recurring] Weekly `#23` maintenance** each Wed/Thu · **September Ops Report** ~2026-10-05.
7. **[Whenever bandwidth / parked]** Gravity Forms 2→3 major upgrade (dev-first) · `#39` Burble Manifest ask still undrafted · E-commerce Merch Discovery (⛔ Burble-tender question) · SCOPE-1 Burble calendar · polish `#1`/`#2` · ADR-0009 open items.

---

## Decisions (Summary)

> Per V-001 (2026-05-18): new decisions land in `docs/adr/`. The `DECISIONS.md` file is frozen 2026-05-18. Pre-2026-05-18 rows below still point at DECISIONS.md sections; post-2026-05-18 rows point at ADRs.

| Date | Decision | Reference |
|------|----------|-----------|
| 2026-04-23 → 2026-04-27 | Phase 1 ratifications: Tommy situation, Rich → Primary SPOC, no-rollback risk, cutover 2026-04-27 9 AM ET, cutover complete, retro location, deploy.sh freeze, ACF replication, roll disclosure | `DECISIONS.md` (sections 2026-04-23 → 2026-04-27 eve) |
| 2026-04-29 → 2026-05-01 | Disable daily-checkin routine; HARN-2 Phase A; CTO standing rule (no PHI to OpenAI without BAA); Phase 2 redesign branch + branching strategy; HARN-5 trial activation; HARN-6 design discarded; TBD-with-flags rejected; HARN-5 trial conclusion | `DECISIONS.md` (sections 2026-04-29 → 2026-05-01 eve) |
| 2026-05-05 → 2026-05-12 | Phase 1 Acceptance received; Matt Pocock skills + GitHub Issues adopted; Managed Services SOW v1.0-DRAFT + canonical glossary; SOW v1.0 issued; Monday → GH migration plan ratified + cutover landed; 5-phase prod-DB-write change-control; SOW v1.1 + MNDA v1.0 drafted then executed | `DECISIONS.md` (sections 2026-05-05 → 2026-05-12) |
| 2026-05-18 | **V-001** — `docs/adr/` wins as decision-recording surface; `DECISIONS.md` frozen | Harness [ADR-0002](../../docs/adr/0002-adr-vs-decisions-md.md) |
| 2026-05-18 | **V-002** — Boot-context split (project-instructions surface vs preferences/feedback surface); non-duplication rule | Harness [ADR-0003](../../docs/adr/0003-boot-context-split.md) |
| 2026-05-19 | **V-003** — `PROJECT_STATE.md` shape (lean resume ≤10 ln + Session Log drain + Watch-out-for triage taxonomy) | Harness [ADR-0004](../../docs/adr/0004-project-state-shape.md) |
| 2026-05-19 | **V-005** — Review pipeline sequencing (`/review` + `/codex:adversarial-review` on judgment gate) | Harness [ADR-0005](../../docs/adr/0005-review-pipeline-sequencing.md) |
| 2026-05-19 | **V-008** — REVIEW vs Retro boundary (REVIEW expanded to agent system optimality across 3 streams) | Harness [ADR-0006](../../docs/adr/0006-review-retro-boundary.md) |
| 2026-05-19 (upd. 2026-08-06) | **V-007** — Issue tracker intake pipeline (`[/wayfinder \| /grill-with-docs]` → `/to-spec` → `/to-tickets` → `/triage` + 4 escape lanes; renamed + wayfinder layer per ADR-0009) | Harness [ADR-0007](../../docs/adr/0007-intake-pipeline-sequencing.md) |
| 2026-05-20 | **V-009** — Recurring task surface (`/loop` intra-session vs `/schedule` cross-session; lifetime-axis split) | Harness [ADR-0008](../../docs/adr/0008-recurring-task-surface.md) |
| 2026-05-19 | V-004 (`/grill-with-docs` over `/grill-me`, doc-only) and 2026-05-19 V-006 (`/review` vs `/security-review` boundary) and 2026-05-20 V-010 (`/status` ↔ `PROJECT_STATE.md` read/write seam) | No ADR (all three failed 3-of-3 ADR-offer test); see [`TOOL_LANDSCAPE.md`](../../TOOL_LANDSCAPE.md) |
| 2026-07-21 | **Design system — Atmospheric Velocity: dual-mode (audience-split), git-canonical 3-tier semantic tokens, Stitch/DesignSync downstream** (ratified seed→authored). Captures grill Q1–Q7. First *product* design-system ADR. | [ADR-0004](https://github.com/itsginfo/skydivecity-com/blob/develop/docs/adr/0004-design-system-atmospheric-velocity-dual-mode-tokens.md) |

---

## A2A Handoff Queue

*(None)*

---

## Session Log

> One row per session. Per V-003: detail lives in the linked ADR / commit / wiki, not the row.

| Date | Agent | Summary |
|------|-------|---------|
| 2026-09-02/03 | PM Agent | **August ops report COMPLETE-but-HELD · Tier 1 approved (§7 still open) · Winterfest additions SHIPPED · three new issues.** (1) **`#41` August report** built end-to-end and QA'd (`049ead5`) — Section 2 pulled over SSH not the broken browser, with the **~7-day Redirection retention** stated as a scope limitation; two apparent defects verified as non-defects. Section 4 liveness clean all 31 days; the month's finding is **tandem flat (357 vs 354) while total transactions fell 48**, so the drop is add-ons. **HELD unsent** for Rich's Burble count (James's call) — decision date ~09-04. (2) **`#34` Tier 1 APPROVED to proceed; §7 compensation still OPEN** — SOW stays DRAFT. (3) **`#43` NEW** — T2/T3 end-to-end LOE, **~155–265h full site**, estimated per **archetype × count** off a verified 56-page inventory; the load-bearing line is that T2+T3 cover ~7× the pages of T1 for about the same effort. **Self-correction:** my "duplicate FAQ pages" finding was **wrong** — they are AFF vs Tandem FAQs at different paths; caught by James asking whether the pages were confirmed live, which also surfaced **13 orphaned pages** (3 USPA-2020 behind a 404 parent, 10 coach pages unlinked from their own parent). (4) **`#44` NEW** — stale live content; **James: drop published rates entirely** rather than refresh them. (5) **`#42` Winterfest** — lodging, fleet and vibrant palms **LIVE on prod** (5-phase, `8cca318`). **Second self-correction:** I built the fleet section from the live facilities page and inherited two errors (Caravans called seasonal, Cessna "182"); James corrected both. **Casa withheld behind a build-strip flag, not an HTML comment** — verified 0 occurrences in the delivered page. Headshots **blocked**: no CSS treatment makes a light-background photo seamless on a dark page (artifact `20251e9a`); cut-out-as-WebP from the **Media Library** is the only viable route, and the call needs 4–5 *worst-case* photos first. (6) GitHub/SDLC meeting prep written and narrowed to James's two-item scope, carrying the **do-not-transfer-the-repo** finding. Wiki: [[flywheel]] gained the **`ssh -tt`** requirement (a missing pty mimics an outage — nearly caused a wrong escalation), the randomized prod table prefix, and transient-255 guidance; [[wp-acf-rendering]] gained the build-time inlining ceiling. |
| 2026-09-01 | PM Agent | **`#40` Flockfest Burble registration link SHIPPED to prod on its open date (5-phase clean).** URL arrived from James via Matt — `https://bookings.burblesoft.com/index/53/1561`, verified against Burble to be the **Flockfest Vol. 2** calendar showing the **$249** early-bird, so link and copy agree. Dev-first (`6b1e7fc`) → prod (`9206578`, log `…-prod-20260901T155439Z.log`): one post written (**5866**, 1741→1676 chars + `ticket_website`), Winterfest/Flamingofest no-ops. A single content change yields **both** surfaces — the body *"Register for Flockfest here!"* anchor **and** the Events Calendar **Tickets** button. Also retired the 150-cap line's *"so be ready when registration opens"* tail, which read wrong once booking was open → *"so secure your spot early."* Live-verified cache-busted (200, 0 fatals, stale notice gone, $249/$275 intact); regression smoke clean across `/`, `/winterfest/`, `/events/`, `/event/flamingofest-2027/` — **Flamingofest deliberately untouched** (Oct 1 notice, no ticket button, empty `ticket_website`). **Correction worth keeping:** I first diagnosed the prod-write failure as a Flywheel gateway outage matching `W4-13` and was about to escalate on a support reference ID. James's own successful manual login exposed the real cause — **the gateway requires a pty for the site-scoped user**; without `-tt` a non-interactive `ssh` completes the publickey handshake and *then* returns *"We're having trouble connecting to your site"*. Not an outage, and **not** James's new external IP (an IP gate cannot survive the handshake). Wiki'd in [[flywheel]] with the `-vv` tell for distinguishing the two, the different *"you must specify a site"* failure when the user is omitted, and a flag that **`deploy.sh`'s post-sync `ssh` (~line 190) lacks `-tt`** and would hit this if `#3` unfreezes it. |
| 2026-08-27 | PM Agent | **`#40` Cassie event updates — SHIPPED to prod, 5-phase clean.** Inventory finding: all three 2027 boogies **already existed** as `tribe_events` (prod 5865/5866/5867; dev IDs differ → updater resolves by **slug**), and **`/event/winterfest-2027/` 301s → `/winterfest/`**, so the Compass landing page is Winterfest's real surface. Built `migration/wp-event-update-boogies-2026-08.php` (idempotent, `DRY_RUN`-default, slug-resolved, image sideload). **Winterfest:** +Kristie Earley (Wingsuit), +Chris Bess (Media), swag sentence → Cassie's exact shirt wording; same edits to `winterfest.body.html` → rebuilt `template-winterfest.php`. **Flockfest / Flamingofest:** "SAVE THE DATES" placeholder → full blurbs, pricing ladders, requirements, LO lists (**Flockfest LOs transcribed from her graphic**) + featured images. **Also fixed:** the live Winterfest page had marked the **expired $99 tier "On now · Best value"** since 2026-07-31 — ribbon moved to the live $135 tier, $99 greyed via the existing `.is-past` style (James: keep). Dev-first → 5-phase prod (SHA-matched upload, host backup of the template, logged execute, cache-busted live verify: 3×200, shirt copy ×3, zero "swag", 0 fatals). Commits `ebac427` + `a0ea18d`; log `…-prod-20260827T172838Z.log`. **Then, same session, a second prod write (`53b65b1`, log `…-175047Z.log`):** (a) **Cassie confirmed the banner swap for BOTH surfaces** → Events Calendar featured image (5865 → attachment **5909**) *and* the landing-page **hero key art** (the old `winterfest-keyart.jpg` was the July banner; replaced with her graphic converted to JPEG 1600w q82 — template 798KB → 756KB) ; (b) **registration-opens notices** added where the register button will go, since neither Flockfest nor Flamingofest has a Burble link yet — **Flockfest opens Sept 1, Flamingofest Oct 1** (James; *different dates*). Re-verified live: all surfaces 200, 0 fatals, new key art inlined, tier order intact. **Open:** wire the 2 Burble links on their open dates → Live Watch. Also corrected a PROJECT_STATE drift (`develop` was **not** 5 commits unpushed) and two tracker gaps: **`#39` + `#40` were missing from GH Project #1** — added + set In Progress. Root cause worth keeping: **`gh project item-list` silently truncates at 30** and the board is past that, so the boot-sequence command was hiding items → `--limit 200` now pinned in `CLAUDE.md`. Wiki: [[wp-acf-rendering]] gained an **"Event content surfaces"** section (the Winterfest 301, build-time-inlined key art, dev↔prod ID divergence). |
| 2026-08-26 | PM Agent | **SOW package rounded out (visual companion · LOE + milestone timeline · actual-AI-LOE swap) + Cassie event-updates request queued (`#40`).** (1) Built a **client-facing visual companion** to the SOW (`redesign-sow-visual.html` — Tiers/Phases/Modules/outcomes; self-contained + private Artifact `99601b51`; commit `5b09ccd`). (2) Rendered the **SOW draft → PDF** (Chrome headless; `6e0c3a6`). (3) Added **§4.3 good-faith LOE + §4.4 milestone timeline** (weeks-from-kickoff) per Phase-1-SOW client feedback (`351e9e8`). (4) **Standing principle (James):** present ITSG's **actual AI-accelerated LOE**, not agency professional-equivalent hours → swapped §4.3 from ~120–210h to **~75–125h**, added a superseding note to `redesign-exec-summary-loe.md` §4 (`2883056`); saved to auto-memory. (5) Captured the **SOW structure convention** (16-section + timeline + LOE) to auto-memory. (6) **NEW routine request `#40`** — Cassie's Winterfest/Flockfest/Flamingofest content updates; graphics staged at `event-content/2026-08-intake/`; **to work 2026-08-27**. ⚠️ SOW PDF is **stale on §4.3** (James generating it another way) — re-render before any handoff. 5 commits unpushed. |
| 2026-08-24 | PM Agent | **Site Redesign (T1) Project SOW `v1.0-DRAFT` drafted (`d472f66`).** Full 16-section Project SOW adapted from the Managed Services SOW instrument; scope/objectives/assumptions from `redesign-exec-summary-loe.md` + PRD `#24`. Per James: **§1 (dates) + §7 (compensation) left `[TO BE DETERMINED]`** (James shaping compensation with Rich per affordability); **§10 IP = full Client ownership incl. an explicit assignment of the Compass Design System** to Skydive City. For internal review before Client; `#34` commented. |
| 2026-08-21 | PM Agent | **Weekly maintenance run (CLEAN) + BM agenda SENT + Rich's homepage content direction logged.** (1) **Weekly maint `#23`** — backup gate met (James confirmed nightly Flywheel backup); applied **12 minor/patch plugin updates** on prod via SSH (ACF Pro 6.8.8, Events Calendar 6.17.3, Yoast 28.3, +9), **HELD Gravity Forms 2.10.5→3.0.3** (major, per D2 + James); smoke PASS (all key pages 200, 0 fatals, 33 active plugins intact); contact-form test PASS (James submitted `WEEKLY-TEST 2026-08-21` manually — **claude-in-chrome screenshot/JS/click tools still broken this session**, `chrome-extension://` conflict — arrived at qa@, debug events=0). Log `maintenance-logs/2026-08-21-run.md`, `#23` commented, commit `2b78bac`. (2) **BM working-session agenda SENT** by James (`correspondence/2026-08-17-…-agenda-SHARE.md`) + requested a time next week — **awaiting BM's date**; verified the agenda content still aligns (open items #30/#35/#37 = agenda items 2/3/4; #33 hosting now a confirm). (3) **Rich homepage content direction logged** (`project_management/redesign-homepage-content-input.md` + `#24`/`#32` comments, commit `57c16e7`): sell the *experience* (people/culture/vibe), Google-Reviews social proof, swoop-pond/sunset/palm atmosphere, taglines "World's Friendliest Dropzone" + "World's Best Skydiving School"; pricing "starts at" still with Rich (`#32`). Both repos **pushed** (`develop`→`57c16e7`, harness `main`→`0889050`). No ADR (routine + comms + content input). |
| 2026-08-20 | PM Agent | **Burble Manifest embed — dev mockup built; root-caused as a Burble-side cookie blocker (`#39` filed).** Rich asked to add the public Burble Customer Manifest board (`us-displays.burblesoft.com/cmp?dz_id=53`) to the site; James refined → load **inline below a button** (not new tab), good UX. Built dev-only: WP page **5892** (`/live-manifest-mockup/`) + `template-manifest.php` (mirrors FFP/Winterfest template pattern; theme `mywp_wrapheader()`+`.section-builder` wrapper) — intro + "Load Live Manifest" button → lazy-injected inline iframe + loading state + first-party fallback link. **Blocker root-caused (verified):** Burble's display session cookie is `Secure; HttpOnly` with **no `SameSite`** → withheld from cross-site iframes → "Login failed". Reproduced on HTTP *and* HTTPS (stood up a cloudflared quick-tunnel to isolate — HTTPS didn't fix it, so not our HTTP-localhost dev); mobile app works only via native WebView first-party context. Fix is Burble-side (`SameSite=None; Secure`) → wiki'd in [[burble-integration]]. **Two corrections logged:** (a) my first "not embeddable / kiosk-only" call was WRONG (it's public); (b) I called builds "done" without visual verification — **browser screenshot + JS-exec tools were broken all session** (`chrome-extension://` conflict), so layout/overlap fixes went unverified (handed to James). Commit `d04c9bc` (**not pushed**). Tunnel + temp static server torn down; autoptimize/10web toggled during diag then restored; unused shortcode mu-plugin removed. **Next:** draft the Burble ask; James verifies layout. |
| 2026-08-20 | PM Agent | **`/wayfinder` map CHARTED for the redesign + 6/11 decisions resolved.** Charted decision map **#27** + tickets **#28–#38** (sub-issues, on Project #1, linked from #24). Fired 2 research subagents → **#28** cross-domain analytics (achievable-partial; Burble supports operator GA/GTM, Omnyra=vendor ask, verify `_gl`, UTM fallback) + **#29** which **cracked the frozen #3 deploy mystery** (17K delta = plugins-tree metadata churn under whole-tree `--delete` rsync; M1 = scoped `--checksum` theme-only rsync; cross-linked #3). Created + resolved **#38** deploy mechanism (Flywheel support confirmed: **no native git push → GitHub Actions→SSH**). James resolved **#33** (Flywheel stays), **#31** (no SEO owner → ITSG owns *preservation* not growth), **#36** (SEO approach: map pre-launch + track go-live via GSC). Added the **staging cutover option (iv)** to #30 (4 options now weighed); **routed #32 pricing + #34 SOW OFF the BM agenda** (Rich track; SOW never named to vendor — prep tightened). Built + updated a **visual decision-map Artifact** (`631f1da6`). Commits `65296fe`→`4eda60e`. **Open:** BM session #30/#35/#37, Rich #32/#34. |
| 2026-08-17 | PM Agent | **Beyond Marketing working-session PREP built + pressure-tested.** Recalled Rich's outstanding pricing item (Q5b "starting at" copy/anchor). Built the BM working-session prep: internal cheat-sheet (`correspondence/2026-08-17-bm-working-session-prep.md` — agenda, per-item ITSG positions, 2 exposures [unsigned SOW · frozen deploy], tough-Q rebuttals) + a send-ready one-pager agenda (`…-agenda-SHARE.md`). Pressure-tested talking points w/ James: corrected #4 (Winterfest already live), armed the "why a new theme vs page-templates" rebuttal (live pages are 469/779/358KB inlined monoliths, no component reuse / no editable content model). **Meeting rescheduled to next week (date TBD).** Commits `3797b3d`→`f42cc34`. |
| 2026-08-14 | PM Agent | **Site Redesign UNBLOCKED + `#24` triaged → `/wayfinder` teed up.** (1) **Beyond Marketing cleared the SDLC** — James La Barrie reply 2026-08-13: risk-tiered review framing landed ("that structure makes sense"); BM scheduling a working session + raised 6 agenda items (SEO/M5, theme-switchover, analytics boundary/M4, Kevin involvement, Flywheel host, Winterfest date) → logged + mapped in `correspondence/2026-08-13-beyond-marketing-sdlc-reply-inbound.md` (`0c56caa`). (2) **Reminded Rich's pricing-messaging** framing still outstanding = Q5b "starting at $X" copy/anchor (technical approach locked; business copy pending Rich). (3) **`/triage #24`** (skill) → `enhancement`/`project-work`/`ready-for-human` (was `needs-triage`); posted a decision-map comment. Key call: BM's reply added real fog (theme-switchover cutover, Winterfest date, Flywheel, analytics boundary) → **next step is `/wayfinder` not `/to-tickets`** (ADR-0009 entry-fork). Phase 0 M1/M2 proceed in parallel (unfogged). Resume repointed (`f54c97a`). |
| 2026-08-06 | PM Agent | **July Digital Ops Report FINALIZED + SENT (`#26` closed).** Rich provided Burble figures (493 tandem − 1 cancellation = **492 net**, **$162,446.75**). Filled §4 reconciliation as a directional ratio: GA4 online funnel **361 purchases / $125,654** = **6.8% open-to-purchase** + **~72% online-capture** of tandem bookings (first clean full-month baselines; scope caveats noted, facts-only). Per James's review, added a **% of funnel opens** column to the §4 table, expanded §5 Ongoing Maintenance (weekly plugin updates + contact-form validation) and Project-work note (Compass DS + Winterfest + FFP). Rendered PDF (installed `poppler` for visual QA — now standard each month), synced the delivery email, **James sent** (To Rich, CC Matt). Commits `7526363`→`e6fd872`. Report + email in `reports/digital-ops-monthly/2026-07.*` + `correspondence/2026-08-06-…`. |
| 2026-08-06 | PM Agent | **Tooling: adopted Matt Pocock skills v1.1 (`/grill-with-docs` decision, Q1–Q7).** `to-prd`→`to-spec`, `to-issues`→`to-tickets` (renames of ratified V-007 tools); NEW **`/wayfinder`** as a PM-owned multi-session decision-map layer. Entry fork: wayfinder (>1 session/foggy) OR grill-with-docs (1 session/clear) → `to-spec` → `to-tickets` → **our** V-005/V-006 back half (NOT upstream `implement`/`code-review` — deferred). Wayfinder labels orthogonal to triage labels; map on Project #1; PROJECT_STATE points to map; don't handcuff wayfinder's native flow. **New [ADR-0009](../../docs/adr/0009-wayfinder-decision-map-layer.md)** (extends 0007); V-007 updated in place; living-doc rename sweep (historical rows preserved). Skills swapped in `~/.claude/skills` (repo v1.2.3); old pair archived `~/.claude/skills-archive-pre-v1.1`; backup `~/.claude/skills-backup-2026-08-06`. Commits harness `c671fb4` + skydivecity `cf2bdf2`. **First wayfinder use teed up: Site Redesign #24 T2/T3.** Open items (in ADR-0009): optional `setup-matt-pocock-skills` refresh; deferred `implement`/`code-review` verdict. |
| 2026-08-05 | PM Agent | **BM SDLC reply drafted+sent · July Ops Report near-final (awaiting Burble count) · Claude account consolidation gmail→itsginfo COMPLETE.** (1) **Beyond Marketing SDLC reply** to James La Barrie drafted (`correspondence/2026-08-05-beyond-marketing-sdlc-reply.md`, `3797b3d`) — answers "how much engineering?" with roadmap scale (not commercials), confirms ITSG owns git setup, reframes review as risk-tiered; cut a defensive "commercials" para per James ([[feedback_answer_only_what_was_asked_in_comms]]); **James sent it**. (2) **July Digital Ops Report** built (`2026-07.md`, `1cced30`): §1 uptime 100% · §3 indexed **↑~417** (post-migration decline reversed) · **§4 liveness ✅ first clean full month — 361 purchases / $125,654, funnel firing all 31 days** · §5 (6 routine reqs + "Project work" note) · §6 careers rec + SSL verified →2026-10-04 · §2 404 window expired. GA4/GSC pulled via browser (itsginfo account). **ONLY §4 reconciliation pending Rich's July Burble count — request SENT** (`correspondence/2026-08-05-burble-july-count-request-rich.md`). (3) **Claude account consolidation gmail→itsginfo COMPLETE** (SIMPLE RE-HOME; Team rejected on cost): Max on itsginfo · CLI+Chrome-MCP switched · Compass/FFP/Winterfest re-published under itsginfo (`f9921e1d`/`d87839ba`/`69456817`) · gmail Max cancelled → Free 2026-08-22 (not deleted). Checklist `~/claude-account-migration-checklist.md`. Also `2d89825` (regen ffp/winterfest.html). No ADR. |
| 2026-07-27 | PM Agent | **FFP Phase B verified live for visitors (post-flush) + merch-discount disclaimer shipped (Rich request).** (1) James flushed the Flywheel cache → **verified Phase B live for real visitors** (no cache-buster): homepage nav one row w/ Rewards no-wrap, teaser renders, CSS `?ver=3.0.3`, 0 errors. (2) **Rich requested a merch-discount disclaimer** — clarify the discount is for the status-holder's own purchases, not bulk-buying for the whole DZ. Added in **two places** (James's call): a *"Merch discount is personal"* card in the ground-rules section (06) + a fineprint note under the rewards matrix (03). Iterated wording w/ James: softened tone, and **merch-only** (dropped "gear" — gear is a future perk, not offered today); also simplified the ground-rules lede *"Four things"*→*"Things to know"* (no count to maintain). Built dev-first (rebuilt `template-ffp.php` via `build-ffp-wp.mjs`), prod deploy SHA-verified `scp -O`, James flushed cache, **verified live for visitors** (`x-cache: MISS,MISS`; both spots present, stale copy gone, 0 PHP errors). Commit `f6d7062`; `#25` commented. Rollback = git `HEAD~1` template (prod-side backup no-op'd; git path confirmed). No ADR (post-launch copy). |
| 2026-07-24 | PM Agent | **FFP Phase B DEPLOYED to prod — homepage teaser + Rewards nav item + legacy-nav fit fix.** Shipped the FFP homepage awareness section + nav entry (5-phase, dev-first): (1) **homepage teaser** (Option B "Jump more. Pay less.", Rich-approved 2026-07-24) inserted above the front-page staff section via `migration/wp-ffp-homepage-teaser-2026-07-20.php` (finalized to Option B + a version-independent idempotency marker; real insert rows 11→13, row-level backup) → CTA to `/frequent-flyer-program/`; (2) **"Rewards" main-menu item** (FFP page 5898) added to the site nav; (3) **legacy theme-nav fit fix** — the 6th top-level item overflowed the float-based `#header` (menu wrapped below the logo, ~14px over at every desktop width since the content area is max-width 1280); tightened the desktop rhythm (menu gap 30→20, link pad 10→8, CTA font 16→14 + pad 25→16, margins) so all 6 fit inline down to 1280px (27px clearance), mobile hamburger untouched; **theme version 3.0.2→3.0.3** to cache-bust the CSS (`?ver` is the theme version). Dev-first: measured the overflow in-browser, then prod deploy (DB backup `/tmp/pre-phaseB-*.sql` + style.css snapshot + SHA-verified `scp -O`); verified live cache-busted (nav no-wrap on prod, teaser renders, Rewards in nav, 0 PHP errors). Commits `a90736f` + `5965a69`. **Pending:** James flushes the Flywheel page cache for visitors. `#25` commented (Phase B complete). No ADR (deploy + CSS fix, not an architectural decision). |
| 2026-07-23 | PM Agent | **Post-deploy QA fixes + unlisted `/compass` showcase + Compass stakeholder email drafted.** After the FFP/Winterfest prod deploy: (1) drafted the **Compass stakeholder email** — SDC-internal (To Rich/Matt/Cassie); **Beyond Marketing deliberately excluded** (delicate vendor-relationship + pending SDLC adoption); narrative reframed to *compass = guidance/alignment* per James; links the two live pages. (2) **Post-deploy QA copy fixes to the live FFP page** (each rebuilt + SHA-verified `scp -O`): No-Membership chip → inline `<strong>August 1st</strong>` (inner `<b>` had inherited the block/uppercase `.feat-chip b` title style, orphaning a trailing period) `74c0ce2`; "where your money goes" → Rich's literal `it's`→`is` (I over-corrected to "we're"/"it is" first; lesson → [[feedback_apply_client_copy_verbatim]]) `0d444be`. (3) **Unlisted `/compass`** design-system showcase deployed (prod page **5900**; new `build-showcase-wp.mjs` + `template-compass.php`; Yoast noindex+nofollow, unlinked, out of sitemap; standalone-assets mu-plugin extended) `baee566` — a branded, stable link vs the claude.ai artifact. (4) **Fixed a dual-mode token bug** the showcase surfaced: `--card-bg`/`--ffp-matrix-bg` (declared once at `:root` as `var(--surface-section)`) don't re-resolve inside a nested opposite-mode pane → Conversion notices rendered dark-on-dark; added a `[data-mode]` re-resolve rule (showcase-only `altitude.css`; FFP/Winterfest immersive-only, unaffected) `5221bb4`. All pushed to `develop`. **Pending:** one Flywheel panel cache-flush serves the FFP copy fixes to visitors. No ADR (bug fixes + copy). |
| 2026-07-23 | PM Agent | **Compass FFP + Winterfest PRODUCTIONIZED and DEPLOYED to prod (5-phase, verified) + redesign exec-summary/LOE delivered.** Business green-lit deploying the redesigned FFP + Winterfest pages (+ good-faith SOW alignment). Built both dev-first as generated WP templates on the live `mywp` theme (`build-ffp-wp.mjs`/`build-winterfest-wp.mjs` wrap canonical Compass output in WP head/GTM scaffolding, strip the provenance banner; header/footer = Compass built-in chrome, Option A); QA'd at localhost:8080 desktop + mobile. **Changes:** unified nav (real URLs + all 6 items incl. Rewards; logo→homepage; **mobile hamburger** — shared `.navtoggle` + solid dropdown); Winterfest "Back to all events" link; **Bloom newsletter fly-in suppressed** on both landing templates (`ffp-standalone-assets.php` v2 — scoped to `et_bloom_` popup classes after a too-broad `[class*="et_bloom"]` rule briefly hid the whole page, since Bloom puts a bare `et_bloom` class on `<body>`); **evergreen `/winterfest`** + 301 `winterfest-redirect.php` (`/event/winterfest-*` → `/winterfest`). Committed `1a7c903` → `develop`. **Prod deploy (5-phase):** DB backup (`/tmp/pre-compass-20260723T230302Z.sql`) + live-template snapshot for rollback → 4 files SHA-verified `scp -O` (2 templates + 2 mu-plugins) → Winterfest page created (prod ID **5899**, slug `winterfest`; FFP page 5898 picked up the swapped template) → verified cache-busted in-browser: both render (no body-hidden bug), `/frequent-flyer-program/` + `/winterfest/` = 200, `/event/winterfest-2027/` → 301 `/winterfest/`, GTM firing, 0 PHP errors. **Follow-up:** Flywheel full-page/CDN cache has no CLI flush → James purges via the Flywheel panel so visitors get the new FFP (old page was cached from Phase A). **Also:** redesign **exec-summary + LOE** delivered (`redesign-exec-summary-loe.md`, `9949890`) — 3 pricing bases, fixed-fee ~$24–25k recommended. No ADR (extends ADR-0003/0004). |
| 2026-07-23 | PM Agent | **FFP feedback round 2 (Rich) applied + client-APPROVED; pushed (`162e89d`).** The R1 email was sent to Rich (Compass showcase link `ef0fe125` + FFP HTML attachment); Rich reviewed and returned R2 notes on the FFP page, all applied and signed off ("good with all the updates"): removed the hero "Book a Jump" button; surfaced the **Aug 1 cycle start** up top (bold in the Ladder lede + added to the "No Membership" hero chip); worth-card labels → "$X &middot; your average per jump"; megastat tail reworded (dropped the "season-end party" quip); cut "you know your numbers better than we ever will" from the Claim lede; **rebuilt "Where your money goes"** from Rich's proto (expanded safety paragraph + mentorship/teams/charities/facility bullet list + "scalable" closer), with the "Why we did this" section now stacked full-width (new `.why-list`/`.why-intro`/`.why-close` styles). Review artifact `620c3c07` (private view only — the branded FFP page can't be shared publicly per the content gate, so it ships to the client as the self-contained HTML attachment `compass-mockup/ffp.html`). No ADR (extends ADR-0004). |
| 2026-07-22 | PM Agent | **Design-system name FINALIZED → "Compass" + business feedback round 1 applied (design system + FFP), pushed (`fc23843`, `e049cc5`).** Business feedback across ~6 review rounds, decided-by-seeing via a comparison Artifact (`c2cba55a`). **Compass (design-system) changes:** jump-counter meter → **crisp tier bands** (~1.5% feather, `ffp-instrument.track-fill`; supersedes the continuous blend); road-ahead **veil → Bold 92%** (was Medium 65%); **drag-thumb brightness now scales with progress** (dim→bright toward Diamond, JS `--thumb-glow`); meter scale relabeled to qualifying numbers **25/50/100/250/400/500+**; **removed the "Paid so far" stat card** (Saved+Avg remain); **Unlocks-Matrix tier-name headers colored to tier**. **FFP-page changes:** opening reordered **Ladder→What That's Worth→Jump Counter** (renumbered); removed the "backbone" block; closing line → "Thank you for jumping with us…"; rewards fineprint reworded; closeout-gradient flip **explored then reverted**. Then **NAME LOCKED: "Compass"** (business, was provisional "Gyro") → renamed all current-system refs + `gyro-mockup/`→`compass-mockup/` dirs across the code repo (`e049cc5`); historical records (ADR-0004, discovery, name-explore) + this Session Log kept as-is. **Signoff Artifacts refreshed:** Compass showcase `8c59c060`, FFP `ead0c4a0` (working preview `50087b95`). **Client-share finding:** artifacts updated cross-conversation via `url` won't share publicly; even a fresh FFP publish (`620c3c07`) is blocked by the public-share gate (branded real-org page — logos + real contact/domain links), while the abstract **Compass showcase shares fine at `ef0fe125`**. → the **FFP page goes to the client as a self-contained HTML attachment** (`compass-mockup/ffp.html`); showcase as the `ef0fe125` link. Email change-lists (FFP + design-system) drafted To Rich / CC Matt. No ADR (extends ADR-0004). |
| 2026-07-22 | PM Agent | **2nd redesign showcase (Winterfest) built on Gyro + FFP footer matched live + BM roster corrected; both repos pushed (`8a8bae6`, `daabefb`).** (1) **Winterfest event landing page** built ON Gyro as a second signoff showcase (after FFP) to help sell the full-site redesign — `project_management/winterfest-landing/gyro-mockup/` (`build-winterfest.mjs` mirrors the FFP harness; reuses `ffp.css` component vocabulary + shared nav/footer/closeout so both showcases read as one system). Content grounded ONLY in the live Winterfest listing (`migration/wp-event-update-winterfest-2026-07.php` heredoc) + staged graphics (`event-content/2026-07-intake/`) — no invented content; deliberately omitted Summerfest's schedule/vendor/sponsor blocks (no content for them). New `wf-` components: framed hero key-art (bright promo art tamed toward the dark palette), **live countdown → Dec 30 2026 8:00 AM ET** (Eastern-anchored per James, was viewer-local), features chips, stat band, deadline pricing tiers ($99 early-bird "on now" / $135 / $165 / $75 day pass), load-organizers-by-discipline cards, know-before tiles. Published **Artifact `1d5fa51c`** (`winterfest-v1`→`countdown-8am-et`, 🎆); `#21` commented (stays `needs-info` — showcase ≠ production pilot). (2) **FFP footer → matches live:** initially edited the WRONG file (stale `design-study/ffp-showcase.html`) + regressed Artifact `ead0c4a0`; traced to the canonical source, applied to `gyro-mockup/ffp.body.html`, rebuilt + republished `ead0c4a0`, reverted the stale file (`8a5575c`); `#25` commented. (3) **BM roster corrected** (James, 2026-07-22) in [[stakeholders]] + memory: James La Barrie (CEO/SDLC decision-maker), Laura Jane Happick (Dir. Web Maint./executes), Marcella Smith (GM/analytics) — SDLC *decision* routes to James La Barrie, not Laura Jane (`daabefb`). No ADR (extend ADR-0003/0004; roster is data). |
| 2026-07-21 | CTO Agent | **Gyro tier-palette + jump-counter refinements, pushed (`4aeb8e8`→`6b51c4b`, 12 commits → `origin/develop` `45f526f..6b51c4b`).** James-driven, decided-by-seeing via Artifacts: (1) tier palette → **literal Platinum `#E5E4E2` + blue Diamond `#9ac5db`** (supersedes ADR-0004 Q4 — ADR amendment + discovery entry; Silver/Platinum proximity surfaced + accepted); light-mode **treatment C** = literal-metal FILL + deepened ink ring/name (new `tier-fill.*` semantic group feeds `ffp-ladder.swatch-*`; `tier.*` stays mode-adjusted ink). (2) Tier-banded jump-counter **ported** mockup→showcase. (3) Showcase content cards → **Section surface** (`--card-bg`; were hardcoding too-plum `--surface-card`). (4) Jump-counter meter → **tier GRADIENT** w/ feathered ~65% progress reveal (no-line rule; `ffp-instrument.track-fill` = tier spectrum referencing `--tier-*` ink vars → mode-flips; added `track-veil`). WCAG 25/25 throughout; both Artifacts republished (`8c59c060`, `ead0c4a0`). Detail: commits + `design-system-discovery.md` "Post-ratification refinements". No new ADR (ADR-0004 amended). |
| 2026-07-21 | CTO Agent | **Gyro system changes promoted from FFP-page findings + FFP nav/closeout/logo/surface iterations (`caf5c17`→`c8a0e86`).** Iterated the FFP-on-Gyro mock with James, then promoted the reusable findings into the design system proper. **FFP mock iterations:** brand logo lockup → nav; replaced an invented CTA with **Rich's approved palm-tree closeout** (verbatim copy from `source/ffp-page-mockup-rich-2026-07-20.html`); circular badge → footer; cards → Section surface (less plum); nav aligned to the **live FFP header** (dark bar). **Then, on James's "anything to adjust in Gyro?":** assessed + he approved 3 → made them real **system** changes (`c8a0e86`): (#1) **nav token** bound to `surface.glass` (a light plum in Immersive) → added mode-aware **`surface.nav`** (Immersive sky-lowest / Conversion paper-card) + rebound `nav.*` + added saturate/border tokens → nav now defaults to the dark bar; (#2) **`card.bg` + `ffp-matrix.bg`** repointed sky-high → **sky-low (section)** (plum fix; ladder zebra preserved; **WCAG re-audited 25/25** both modes); (#3) **brand marks** (lockup/badge/palm) promoted to **`design-system/brand/`** as first-class owned assets + README. FFP mock migrated to consume the tokens directly — **now override-free**; dist + showcase regenerated. **Showcase live Artifact (`8c59c060`) not yet redeployed** — its committed HTML is current (matrix now section) but the live URL is a snapshot; redeploy if we want it to reflect the token changes. FFP Artifact `ead0c4a0` republished + verified. No ADR (extends ADR-0004). |
| 2026-07-21 | CTO Agent | **FFP landing page REBUILT on Gyro + published for signoff (`b623058`).** Executed the queued next-action: replaced the earlier bespoke self-contained FFP study with a mock-up built **ON the canonical Gyro system** — no hand-rolled CSS. `project_management/frequent-flyer-program/gyro-mockup/` (`build-ffp.mjs` mirrors the showcase harness: inlines the shared brand fonts + `dist/atmospheric-velocity.css` + page CSS + body; ASCII-safe, Artifact-ready). Every color/type/elevation/motion value **token-bound**; the real Gyro components (HUD/gold buttons, links, cards, inputs, chips, mode-aware notices, **jump-counter instrument**, tier ladder, unlocks matrix, stat blocks) ported verbatim from `altitude.css`. **Committed single-world Immersive (dark)** per the audience map (viewer toggle intentionally not wired); **rare-gradient rule** honored (one hero gradient). Rich's content-final copy + jump-counter JS with tier math ported verbatim — **verified live in-browser at jumps 120 ($4,190/$370/$34.92) and 210 ($7,070/$910/$33.67)**, and the static worth figures ($250/$550/$1,150/$3,000 saved) reconcile. 0 console errors; 0 non-ASCII bytes. **Published → Artifact `ead0c4a0-d721-4e22-988a-dea447b948e6`** (`ffp-on-gyro-v1`; verified on both local + published surfaces, 🪂 favicon, title tab). Business-dev/cost-of-business; feeds FFP Phase B + the real WP theme template at redesign-SOW time. No ADR (applies ADR-0004). **Refinement (same session, `caf5c17`, Artifact URL unchanged):** per James — added the **brand logo lockup to the nav** and replaced an invented "You're already in it" CTA with **Rich's APPROVED palm-tree closeout** ("The more you jump, the less you pay. It's that simple." / thank-you / "See you on Z-Beach."). Both brand assets are Rich's own self-contained gradient SVG data-URIs, extracted verbatim from `source/ffp-page-mockup-rich-2026-07-20.html` + inlined by the build. **Guardrail honored:** FFP content is client-approved — presentation is Gyro, copy is untouched/verbatim. |
| 2026-07-21 | CTO Agent | **Stitch investigation + doc cleanup + both repos pushed; FFP-mockup queued (`4808cce`→`45f526f`).** James asked how to replicate the showcase into Stitch → clarified **Stitch is a generator, not an HTML importer** (generated a component-gallery screen `2d1354fb` from the theme — nailed the aesthetic but invented copy + a fictional "Weather Glass"/portal). Root cause: `DESIGN.stitch.md` carried seed-era narrative Stitch read as a build brief; **tightened both DESIGN.stitch.md + DESIGN.md** to factual overviews of the as-built system (removed Weather Glass/Portal, poetic "void" prose; added "do not invent features" to the Stitch payload). Ran a full **integrity sweep** (rebuilt all generators → only drift was the 10 preview cards' inlined Gyro banner, committed; WCAG 25/25; 0 stale-name leaks) and **pushed both repos to origin** (project `develop` `93a6160..45f526f`; harness `main` `3b32a33..3471f34` — 30 commits incl. prior sessions). **Next: FFP mock-up on Gyro (James).** No ADR. |
| 2026-07-21 | CTO Agent | **`/design-sync` populated + system renamed → "Gyro" (`41706e2`→`7f919c4`).** (1) **`/design-sync`:** the first-party skill isn't on skills.sh (only unrelated community hits), so met the goal via the DesignSync MCP tool directly — regenerated the 10 preview cards with Aurora tokens, re-uploaded, `register_assets`'d all 10; claude.ai/design library `114eac73…` now shows **10 grouped, rendering cards** (was empty). (2) **Naming:** ideated on a brand-styled masthead board (Altimeter · Attitude · Gyroscope · Gyro · Compass, Artifact `e6494621`) — James chose **Gyro** (provisional, may change post-business-feedback). Content-swept "Atmospheric Velocity" + "Altitude" → "Gyro" across showcase/DESIGN.md/DESIGN.stitch.md/README/contracts/build banners; rebuilt (0 leaks, WCAG 25/25); **Artifact redeployed to the SAME URL `8c59c060`**. ADR-0004 note-only (historical record); filenames + external Stitch/DesignSync names deliberately deferred until the name locks. No ADR. |
| 2026-07-21 | CTO Agent | **Altitude showcase — long iterative refinement pass with James (`8f060cc`→`b041cc8`, Artifact redeployed to `altitude-v6`).** Rounds: (1) **ported the component gallery from the approved mock-ups** (`visual-aids/motion-demo.html`) instead of the fresh variations I'd first written — link underline-wipe, tonal-lift event card, focus-glow input, and the **real-events View Transitions** (Winterfest/Flight-1/Collegiate, gradient thumbs, breadcrumb, shared-element morph). (2) **Jump-counter** → single full-width, live-counter metrics (Paid/Saved/Avg + "Costs you $X"), tier math ported verbatim + verified; fill tracks the slider 1:1 (no lag, via a `dragging` class). (3) **Fixed a real token-generator bug** — `cubicBezier` `$type` lived on the group so `--motion-easing-standard` emitted raw numbers, invalidating every transition (they snapped); `walk()`/`renderValue()` now inherit group `$type` (fixes the shipped WP CSS too). (4) **Input focus** = designed teal bottom-glow only (dropped the global focus-ring box on `.inp`). (5) **Status colors → "Aurora"** (brand-harmonized emerald/amber/rose-red/teal; WCAG re-verified) and **notice panels made mode-aware** (Immersive oklab-10% fill + 3px accent bar to fix muddy dark fills; Conversion restored to the original visible pastel, no bar — mode-flipped custom props). Explore boards `0428c9da`/`6700f6c9`; decisions in `design-system-discovery.md`. **Also fixed the recurring `cd` approval-prompt for good:** `.claude/hooks/block-cd.sh` + a PreToolUse hook (hard-deny `cd`; proven firing). ⏳ Still queued (both James-approved): git-wide "Altitude" rename (full replace) + `/design-sync` install. No ADR. |
| 2026-07-21 | CTO Agent | **Signoff showcase "Altitude" built + published (`6b64194`).** James flagged the Stitch/DesignSync outputs weren't usable for business signoff (Stitch themes only; DesignSync project renders empty — `@dsCard` cards need the `/design-sync` render pass, skill not installed). Diagnosed honestly, then **pivoted to the correct surface: a self-contained visual showcase Artifact.** `design-system/showcase/` + `build-showcase.mjs` inline the brand fonts (Space Grotesk/Work Sans/IBM Plex Mono as data-URI @font-face — Artifacts block font CDNs) + canonical tokens + a designed long-scroll body. The **viewer's light/dark toggle flips the whole doc Immersive↔Conversion** (dogfoods the dual-mode tokens live); live hero jump-counter instrument; color/tiers/type/components/FFP/rules sections. Fixed an encoding bug (non-ASCII → numeric entities) + verified both modes + FFP components in-browser. **Published → Artifact `8c59c060-eeeb-4fdd-ac0d-c4eb2b3d00e3`.** Also: renamed the system to **"Altitude"** in the client view per James (git-wide rename from "Atmospheric Velocity" = pending fast-follow). No ADR. |
| 2026-07-21 | CTO Agent | **Design-system OUTWARD PUBLISH — both targets done (`f8d425e`/`36e8ab1`).** After the git build (row below), James chose "do both now." (1) **Stitch:** authored `design-system/DESIGN.stitch.md` (Immersive-only prose projection — Stitch is single-mode + parses prose, so the dual-mode `DESIGN.md` would muddy extraction), `upload_design_md`→`create_design_system_from_design_md` → **new asset `d57ad0b4da0c4f9abf43385f5e898522`**. Deliberately NOT applied as the active theme (April "Apex Altitude" `922f1b31…` stays applied; existing screens untouched — James can apply in the UI). (2) **DesignSync:** wrote `build/build-previews.mjs` → 10 self-contained cards (`previews/*.html`, `@dsCard` markers, same semantic tokens the WP theme will use); created project **`114eac73-811a-413c-954d-b8d9fba9fb0c`** ("Atmospheric Velocity — Skydive City"), finalize_plan (James approved) → `write_files` → **10/10 live** (verified via `list_files`). Spot-checked tiers + FFP-ladder cards in-browser: dual-mode remap + tier swatches + mono pricing + gold pinnacle all render correctly. Build phase 100% complete; only remaining consumer is the WP-theme port at redesign-SOW time. No ADR. |
| 2026-07-21 | CTO Agent | **Design-system BUILD PHASE landed in git (commit `86eb93d`).** Authored the canonical **3-tier, dual-mode W3C Design Tokens JSON** (`design-system/tokens/`: primitives → semantic immersive/conversion, identical paths + per-mode aliases → components), a **dependency-free Node generator** (`build/build-tokens.mjs`) emitting `dist/atmospheric-velocity.css` (layered `:root`/`[data-mode]` so a mode flip is a pure cascade — zero component edits) + `dist/design-md.stitch.yaml` (Immersive, resolved to hex for the Stitch publish). Wrote a **WCAG 2.1 contrast audit** (`build/check-contrast.mjs`) — **25/25 pass both modes**, quantitatively validating the Q4-light "deepen the pale metals" call (silver/platinum/diamond clear 3.0 on white). Authored **headless-ready component contracts** (`components/contracts.json`) for base + the 5 FFP components (instrument/ladder/matrix/stat/notice). **Reframed `DESIGN.md`** from source-of-truth to the generated human spec (dual-mode, 3 fonts, gradient rule, semantic status, motion/View Transitions) per ADR-0004. Locked **Diamond = solid icy** (Q4-diamond B; superseded the FFP gradient swatch) and gave **Platinum** a distinct brighter-cyan value (superseded the FFP teal placeholder). **Stitch MCP now resolves** (`ScreenInstance $ref` error cleared; `list_design_systems` returns the April "Apex Altitude" seed). Committed to `develop` (not pushed). **Next (outward, confirm first): DesignSync library + Stitch publish.** No ADR (build executes ADR-0004). |
| 2026-07-21 | CTO/PM Agent | **Housekeeping cleared + Task #8 → full Design-System authoring.** (1) Fixed the push/`gh` blocker (`gh auth switch --user itsginfo`), pushed 51 commits, filed FFP retroactive issue **`#25`** (`project-work`). (2) **Task #8 FFP redesign test-bed:** built a self-contained **FFP showcase** on the Atmospheric Velocity design system (Artifact `a13bdcde`; interactive jump-counter, tier math verified vs Rich's figures; repo `frequent-flyer-program/design-study/`, `f3b6d78`). (3) James challenged "how did we finish an artifact when the system's incomplete" → surfaced `DESIGN.md` (Stitch/CMO-2) is a **seed, not complete**; James green-lit fleshing it out. (4) **`/grill-with-docs` on the design system, Q1–Q7 COMPLETE** (`design-system-discovery.md`): dual-mode audience-split · 3-tier git-canonical W3C tokens (Stitch/DesignSync downstream) · ratify Atmospheric Velocity · tier palette (Diamond solid-icy, mode-adjusted) · IBM Plex Mono · rare-gradient rule · hybrid semantic tokens · motion + View Transitions. Visualized each decision live via a **decisions-board Artifact** (`3ed1a308`) + **motion demo** (`2d81b43d`), saved in-repo (`design-system/visual-aids/`). **First product design-system ADR — [ADR-0004]** drafted; `redesign-discovery.md` "not yet authored" reconciled. Connected Stitch MCP (`claude mcp add`) but **tools-fetch fails** (`ScreenInstance $ref`) — non-blocking (git is truth). Commits `54f1581`/`93a6160`. **Next: build phase (own session)** — token JSON → DesignSync library → Stitch publish. |
| 2026-07-17 | PM Agent | **Weekly maintenance `#23` — 2nd run, CLEAN** (caught up the missed week-of-07-15 Wed/Thu window). Backups confirmed by James → applied all **7 pending updates, all minor/patch** (ACF Pro 6.8.6, Events Calendar 6.17.0, Redirection 5.9.0, DB Cleaner 4.2.0, Cookie Law 3.5.3, Filester 2.1.2, Event Tickets 5.29.0.1; Yoast already current 28.0) — no majors held. Client-timeout cut the SSH batch mid-install but the **server completed all 7** (verified on reconnect; no stuck `.maintenance`). Smoke PASS (/, /contact/, /events/, event + tandem pages all 200; 33 active, no deactivation; 0 pending). Contact-form test PASS — browser ext not connected so James submitted `WEEKLY-TEST 2026-07-17` manually; qa@ received; WP Mail SMTP debug events (24h)=0; entry retained. Log `maintenance-logs/2026-07-17-run.md`; `#23` commented. **Also corrected stale email state:** BM SDLC email + weekly-maintenance follow-up both confirmed SENT (per James). Carry: standing reminder for the Wed/Thu window. Next: redesign (blocked on Rich + BM reply). No ADR. |
| 2026-07-13 | PM Agent | **Site Redesign — `/grill-with-docs` discovery COMPLETE (Q0–Q8).** Ran structured grilling against the domain model (CONTEXT.md, wiki [[wp-acf-rendering]]/[[tracking-stack]], ecommerce-merch-discovery). Caught + resolved the "Phase 2" terminology conflict (CONTEXT.md reserves against it) → canonical name **"Site Redesign"**, one Project SOW. Locked: "ecommerce"=Tandem/AFF/Events sign-up integration (merch deferred, decouples blocked merch thread); hierarchy **Tandem>AFF>Events** (Rich's AFF complaint = visibility, not strategy); "5-star"=qualitative CX + 3 measurable layers with **AFF+Events instrumentation as in-scope prerequisite** (only Burble tracked today); **full-site end state, phased** (design system = first-class T1 deliverable); 6 content-model requirements; **Option A new source-controlled WP theme headless-ready** (first product **ADR-0003**; headless deferred as measured future swap, re-platform rejected — protects SEO equity + BM workflow); branded link-out + "starting at" pricing; T1 = Phase 0 enabling → Winterfest `#21` tracer bullet → homepage+spine; lanes ITSG/Cassie-Young/Rich/Beyond-Marketing with **content-owner risk RESOLVED**. Discovery doc `redesign-discovery.md`. 2 external homework → Rich/BM (PRD assumptions, not blockers). Then: **PRD PUBLISHED → [`#24`](https://github.com/itsginfo/skydivecity-com/issues/24)** (`needs-triage`, on Project #1; 32 user stories, modules M1–M7, M3/M4/M5 tested); scope "Out of Scope" split into committed-later (T2/T3) vs excluded (merch/headless/re-platform) after James flagged ambiguity; `#21` designated tracer-bullet pilot (commented). **Stakeholder gap fixed:** misaddressed BM email to Marcella (analytics) → corrected to **Laura Jane** (Website/Mktg Mgr, Phase 1 Plan §8.1); built [[stakeholders]] wiki map + boot Quick-Index pointer + lesson memory. **BM SDLC-workflow email APPROVED** (`correspondence/2026-07-13-…`), grilled + reframed on scale/multi-engineer enablement (LocalWP already gives BM separation+preview) — James to heads-up Rich then send. Commits `b79e9a4`→`5d9a9e4` (project) + `db275df`→`2b44ec0` (harness). First product **ADR-0003**. **Next:** await Rich/BM → `/triage #24` → `/to-issues`. |
| 2026-07-08 | PM Agent | **NEW weekly-maintenance routine `#23` stood up + first run complete + OPERATIONAL.** James added recurring scope: weekly plugin updates + contact-form QA. Locked D1 agent-assisted (sandbox-allowlist blocks headless) / D2 minor-patch-auto-hold-majors / D3 test-route → qa@itsginfo.com (conditional GF notification on `WEEKLY-TEST` marker). Traced contact form = GF form 1 → info@ (`backup.sql` + live prod read). Applied qa@ notification on prod (5-phase, SHA-verified). First run: DB backup → 9 patches applied → smoke PASS → contact-form test PASS (James submitted via browser after finding GFAPI blocked by honeypot/WP Armour; qa@ received). Yoast 28.0 applied by James + verified. Decisions: verify manual (WP Mail Logging inactive), retain test entries for audit. Window = Wed/Thu. Exec-summary follow-up email drafted for June chain. Discovered + wiki'd Flywheel WP-CLI path `/www/.wordpress`; WP Mail SMTP (Gmail) forces From→info@. Runbook `weekly-maintenance-runbook.md` + log `maintenance-logs/2026-07-08-first-run.md`. Commits `9c78d2d`→`63f22ab`. No ADR (operational routine; runbook is canonical home). |
| 2026-07-06 (later) | PM Agent | **June Ops Report SENT.** James approved report + email; marked draft SENT (`correspondence/2026-07-06-june-ops-report-rich.md`, To Rich/CC Matt). Reworked email to May template ("Hi All,"/"The headlines:"/direct-to-Rich count paragraph). Analysis for James: 507-vs-83 don't align — two stacked causes (window: GA4 dark Jun 1–21, saw only ≈9-day tail; **scope: GA4 `purchase` = online-funnel-only subset** of Burble's all-channel reservations) → deliberately did NOT claim "directionally consistent" (June gap wider than May's; 9.2/day vs 16.9/day), attributed to window+scope "not a tracking fault." Flagged residual: even July's clean month won't equal — report as ratio, not equality (carried to July Live-Watch row). Drafts `bf79c02`→`dfdad30`. No prod writes. No ADR. |
| 2026-07-06 | PM Agent | **June Ops Report unblocked + `#13` closed.** Rich provided June Burble count (net **507** tandem reservations = 519 gross − 12 cancellations); folded into §4 as **recorded-not-reconciled** vs GA4's 83 (tracking dark Jun 1–21 → GA4 saw only the ≈9-day tail; two figures not like-for-like — July is first clean month) per facts-only reporting standard. Updated TL;DR + §5 `#13` refs to since-closed; re-rendered PDF (`≈` not bare `~` to dodge the strikethrough gotcha). Commit `233b23b`. Closed **`#13`** media pricing — James relayed Burble-side config complete (site + funnel now agree). No prod writes. No ADR. |
| 2026-07-02 (later 4) | PM Agent | **4 prod writes SHIPPED + live-verified (`#18`/`#17`/`#19`/`#22`); `#20` closed; redesign thoughts logged.** James: close `#20` (done — collegiate logo → separate event, 5761 needs nothing) + "ok to push to prod." Ran full 5-phase for all 4. **Phase-1 inventory caught a dev/prod post-ID divergence** — `#17` hardcoded 5878 = a *revision* on prod; real WINTERFEST = 5865 → fixed script to resolve by slug (lesson: event UPDATES resolve by slug, not hardcoded ID; page-2 core ID stable). SHA-verified all 7 artifacts bit-for-bit. Executed `#18`→`#17`→`#19`→`#22`: prod events **5890** (Flight-1), **5865** (Winterfest content/reg/banner), page-2 band (`#19` AFF button + Learn-More→1098 + prepended hook, SEO preserved), **5894** (Collegiate). Live-verified all: HTTP 200, content, no error strings. Closed `#17`/`#18`/`#19`/`#22`. Commit `4337b5b`. Then gave James redesign-direction thoughts (homepage = IA problem; let clean July GA4 baseline rank CTAs; rationalize content model; preserve SEO; don't let patches become de-facto redesign; `#21` as pilot) — logged in resume. No ADR. |
| 2026-07-02 (later 3) | PM Agent | **Collegiate event `#22` created; `#19` Option A dev-built non-destructively; SSH RESTORED.** James clarified the collegiate logo ≠ Nationals Canopy Piloting (5761) — two separate events → filed `#22` "2026 USPA Skydiving Collegiate Championship" (Dec 28 2026–Jan 2 2027), dev-built as event 5885 from the 2024 ref (5791/prod), verified + idempotent (`27d1730`). Commented `#20`: logo ambiguity resolved, holding `needs-info` on whether 5761 needs anything. Then Option A on `#19` — **discovered the "band body" is a live 5-para SEO/brand block, not a throwaway line** (literal replace would nuke 30yrs/80-countries/Z-Hills copy + internal links); applied **non-destructively**: prepended the AFF hook (SEO preserved) + repointed "Learn More" About(1003)→Learn-to-Skydive(1098); dev-verified vs rendered homepage + idempotent (`38fb4f0`); body treatment surfaced to James (prepend/replace-line-1/skip). **SSH probe → `SSH_OK`** (throttle cleared); 4 prod writes now queued+ready but **HELD for James's go** (away mid-decision, post-throttle caution). No prod writes. No ADR. |
| 2026-07-02 (later 2) | PM Agent | **`#19` home AFF CTA dev-built + copy-enhancement options proposed; paused per James.** Built `#19` on dev — added "Sign Up for AFF"→academy button to the "Best Skydiving School" band; hit + worked around an **ACF orphan-meta gotcha** (page 2 has dead `content_1_formatted_*`/`content_2_left_*` meta from old layouts; live band is `content_3_*` — caught via render check; card-repoint change dropped, no live AFF card exists). Script `131d4a1`. Then (CMO lens) proposed 3 copy quick-wins to make AFF more prominent in that band — recommended Option A (AFF-reoriented body copy) + "Learn More"→AFF-page repoint; **awaiting James's pick.** SSH still blocked (cooldown/re-probe pending). No prod writes (all 3 queued). No ADR. |
| 2026-07-02 (later) | PM Agent | **New content intake from Rich (5 issues `#17`–`#21`); `#18` + `#17` dev-built; hit Flywheel SSH outage.** Rich sent event/content drop → filed `#17` Winterfest content (5878), `#18` new Flight-1 event (Jul 25–26, Aaron Stocum), `#19` home-page AFF visibility, `#20` USPA Nationals CP (needs-info: collegiate-logo/event mismatch), `#21` Winterfest landing page (needs-info→scoping; Summerfest ref https://skydivechicago.com/summerfest/). 6 graphics + `CONTENT-NOTES.md` staged in `project_management/event-content/2026-07-intake/` (`0ab0477`). Built `#18` on dev (event 5881, featured-image sideload, render 200, idempotent; `25f4f80`) — James approved for prod. Built `#17` on dev (5878 content+reg-link+image, render 200; `b4d9abc`). **Prod writes for both BLOCKED — Flywheel SSH gateway resetting key exchange** (site HTTP 200, SSH-only); logged on `#18`, escalation = Flywheel Sr Eng (James). Both queued. No ADR. |
| 2026-07-01 → 07-02 | PM Agent | **June Digital Ops Report drafted content-complete + rendered; delivery paused for client data.** Autonomous pulls: §1 uptime 100%, §2 404 (prod SSH read; retention-limited 06-23→30, scanner noise + 2 minor legacy signals). James exported GA4 + GSC CSVs (`2026-06-ga4/`, `2026-06-gsc/`), no Ads access. §3 GSC: ≈371 indexed (flat vs May 376; decline settled; impressions up). §4 (key): GA4 by-date pinned the dark→live crossover — native purchase/funnel dark **Jun 1–21**, restored **~Jun 22–23** (BM's Option B) → **83 bookings/$32,476 in the ~9-day tail only**, NOT full-month; daily rate ≈9.2/day ≈ May-1–17's 9.8/day (healthy). Carried facts-only May-figure correction (166/$68,298 = May 1–17 pre-fix). Reconciliation deferred to July (first clean full month). §5 (#12/#13/#15/#16), §6 careers-page rec. James approved framing/language. PDF rendered, delivery email drafted (To Rich/CC Matt). **Paused per James: awaiting Rich's Burble count → James feedback → send.** Commits `4338c6c` (scaffold) → `d510783` (complete). No ADR. |
| 2026-06-29 (late) | PM Agent | **`#16` Omnyra AFF→academy repoint EXECUTED on prod (5-phase clean) + CLOSED.** Self-verified cert gate cleared (`academy.skydivecity.com` valid Let's Encrypt cert issued same day, live FJC signup, `/book`+`/portal` 200). Phase 1 read-only inventory (`migration/wp-aff-omnyra-inventory.php`) confirmed prod = dev: 4 AFF-form fields (1099 ×2, 1613 nav, 1872 "AFF CTA" block), 0 academy collisions, 13 event-form posts out of scope, no save-hook risk. Phase 2 SHA MATCH. Phase 3 executed → 4 fields → `academy.skydivecity.com`. Phase 4 verified live (0 legacy refs, academy links rendering, HTTP 200, no PHP errors). Commit `a57748e` (develop, pushed). Careers subdomain DNS-live but unwired → carried as June-report rec. No ADR (routine prod write per established 5-phase procedure). |
| 2026-06-29 | PM Agent | **`#15` CLOSED + `#16` Omnyra repoint staged (held on cert).** James reported both pending items resolved: (1) BM executed **Option B** on `#15` (removed GTM's GA4 config tag → Burble native owns GA4); post-change testing confirmed native events firing → tracking restored. Validated mirror-image model + event-based reporting (B = viable-not-compromise). Commented + closed `#15`. (2) `#16` Omnyra walkthrough done — spec received: tool on Omnyra Vercel infra, `academy.` (AFF signup) + `careers.` (pilot) subdomains; DNS done by James. Dev-first discovery → AFF form in 4 published fields (event forms out of scope). Wrote idempotent repoint script `migration/wp-aff-omnyra-academy-repoint-2026-06-29.php`, **dev-validated** (dry→live→idempotent clean). Reclassified `#16` `needs-info`→`routine-request`; SCOPE-2 retired (not a Project SOW). **Prod write HELD per James** — `academy.skydivecity.com` cert not live yet (Josh to confirm). Careers: skip wiring, carry as June-report rec. No prod writes this session. |
| 2026-06-18 (later) | PM Agent | **`#15` GA4 mirror-image model VALIDATED with live data — call-ready visual built.** Re-validation triple-check (James, pre-Marcella-call): read live native bundle (`/assets/js/js_tracking/google_analytics_4.js`) → native fires funnel + ecommerce events only, **NO page_view** (`send_page_view:false`) → GTM is sole page_view source. Built GA4 Explore ("Burble: page_views vs events", host-filtered) → **page_view dark→on at 5-18, native Add Jumper on→dark at 5-18, crossover** = the incident in one frame. Caught + retracted my own interim error (misread a CSV column — Active users as Views — which had me briefly claim pre-5-18 page_views existed; the Explore pull disproved it; original mirror-image model stands). Framed events = outcome/revenue layer vs page_views = journey layer; Burble = bottom-funnel only → event-based reporting makes Option B viable-not-compromise. **Pivotal Q for the call surfaced:** event-names vs page-paths → decides A vs B. A-vs-B rec deferred to post-call per James. `#15` comment added. No code changes (GA4 UI + read-only curls). |
| 2026-06-18 | PM Agent | **`#15` Option B email sent → Marcella wants a verification call + raised "why is SDC different" (page_view) question; new scope `#16` (Omnyra AI) filed.** James sent the Option B agreement; Marcella agreed and asked to schedule a walk-through-and-verify call. Her reply questioned why SDC's booking page_views differ from her other Burble clients (parked for the call) → triggered the GA4 re-validation above. Separately, Rich connected James with **Josh Caruso (Omnyra AI)** — customer-capture/retention tool for FJC + AFF to integrate on the SDC domain, likely replacing the AFF Google Form signup; filed [`#16`](https://github.com/itsginfo/skydivecity-com/issues/16) (`needs-info`, on Project #1), added SCOPE-2 to Pending Project SOWs. James walkthrough with Josh 2026-06-22. |
| 2026-06-17 | PM Agent | **`#15` BM walkthrough diagram built + Ads conversion-counting open question surfaced.** Interactive HTML data-flow diagram (`correspondence/2026-06-17-tracking-architecture-walkthrough.html`, commit `96c3df3`, not pushed) for James's BM meeting: all elements + 3 clickable states (Before/After/Option-A) + mirror-image matrix, facts-only. James probed the Ads-row claim → surfaced that "Ads conversion dark-before/live-after" is verified only for the **GTM AW tag**; if Ads *also* imports conversions from GA4, that action follows the revenue timeline (alive-before/dark-after) and BM's recollection would be literally true. Logged as open Q on `#15`; diagram Ads row pending Marcella's answer (GTM tag / GA4 import / both). No ADR (verification, not decision). |
| 2026-06-13 | CTO Agent | **`#15` BM reply SENT (James) after native-bundle verification; Option A tightened.** James's Burble admin screenshot disproved the "Ads IDs not in Burble's form" claim → corrected draft line 19, `#15` body Evidence line, + `#15` comment (IDs live in BOTH form and GTM). Verified from the four native JS bundles that Burble's native pipeline fires **no** Ads conversion (only GA4 events; `google_tag_manager.js` finish push gated by `if(isGA4Enabled())return;`) → GTM `/index/finish/` tag is the sole Ads-conversion source (explains Ads dark pre-05-18 / live post-05-18, no double-count). Analyzed Option A data-loss risk → tightened the reply: forward full ecommerce set (not just `purchase`), pin `value`→`ga4_revenue` + `transaction_id` + `items[]`, add test-booking check that native stands down after blanking (empty key ≠ removed for `isGA4Enabled()`). Reply SENT 2026-06-13; awaiting BM remediation response. May-report window correction folded into June report (~2026-07-05). Wiki [[burble-integration]] + [[tracking-stack]] corrected (reversed Defect-2 "dead code" verdict; documented dual-pipeline conflict + `isGA4Enabled()` blank≠removed gotcha). Commits `6eb1f31`/`f143d26`/`f2ecf83` (project) + `3f7982a`/`6aa2b9e` (harness) + session-end. |
| 2026-06-12 | PM Agent | **Sev-3 incident `#15` filed — GA4 purchase tracking dark since 05-18; root cause CONFIRMED.** BM (Marcella) reported revenue/product data gone post-05-18. Investigation: rendered-HTML verify (all 4 tag IDs clean/correct), Wayback diffs (Burble scripts byte-identical Jan→May 22; consent-gating predates Feb), GTM container decode (no consent defaults; AW conversion + GA4 googtag + dup FB pixel), live headless-Chrome repro (consent accepted → only GTM `page_view` transmits; native gtag events swallowed). Verdict: `#9` GTM fix unmasked dual-pipeline conflict — purchase event lives only in Burble's native pipeline, which `#9`'s Defect-2 wrongly called dead code. May report window misattribution found (166/$68,298 = May 1–17, not post-fix). Reply to Marcella drafted (`correspondence/`), Option A recommended. `#9` Live-Watch row retired into `#15`. |
| 2026-06-11 (later 2) | PM Agent | **`#13` live-verified uncached (edge cycled ~35 min) + `#14` closed not-planned.** Rich's `#14` screenshot was CSC's Burble page, not SDC's — "Display lowest prices available in calendar" isn't enabled on our funnel, so nothing to restyle; preference folded into the Dynamic Pricing carry-along. `#13` open only on Matt's Burble-side confirmation. |
| 2026-06-11 (later) | PM Agent | **`#13` media-package pricing EXECUTED on prod — 5-phase CLEAN.** Rich confirmed CC stays +$10: Hand Cam $125/$135, Outside $150/$160, Ultra-Premium $225/$235; orphaned `bookinglink` 5508 → "Starting at $125". SHA-verified (`84ab942a`), 7/7 fields updated, idempotency re-run clean on dev, DB verified on both pages via cache-busted curls (Cloudflare edge clears within a cycle). Script `wp-media-pricing-update-2026-06-11.php` (`4c242e0`, pushed). `#13` open only on Matt's Burble-side confirmation. |
| 2026-06-11 | PM Agent | **Rich's media-pricing request intake — `#13` + `#14` filed (routine-request escape lane).** Inventory dev-first + prod read-only (`wp-media-pricing-inventory.php`, `d0b836c`): media prices live only on `pricingtable` 5613 (Cash/CC × Hand Cam/Outside/Ultra-Premium), rendered on `/skydiving-prices/` + `/media-packages/`; `bookinglink` 5508 orphaned. `#13` needs-info: Cash-vs-CC column treatment for the new $125/$150/$225. `#14`: Burble timeslot price-next-to-time CSS. **Dynamic Pricing Calendar ON HOLD per Rich** (monitor media-price impact first; sheet updated with red notes). Scratch tandem-bookings files committed + `.claude/` gitignored (`a776885`). |
| 2026-06-10 (later) | CTO Agent | **Harness-scoped session — `ai-agent-harness#9` + `#10` closed; no SkydiveCity changes.** QUICKSTART.md rewritten as the canonical ADE project-onboarding doc (reading order, 3-artifact override pattern, SkydiveCity + MethodRX reference shapes); MethodRX tracker drift reconciled across all 3 surfaces (`EQ-Labs-LLC/method_rx`); Monday-as-default + stale model refs swept from 21 harness files (HARNESS.md v1.5, COMPANY.md v2.1, 6 protocols, 9 agent files, 2 integration guides); `integrations/MONDAY.md` marked legacy/opt-in. Commits `bc0dce8` + `a65d8b0` (the latter also carries the parallel MethodRX session's PROJECT_STATE update, included as-found). No ADR (fails 3-of-3). |
| 2026-06-10 | PM Agent | **Harness-scoped session — no SkydiveCity deliverables.** (1) MethodRX/ADE onboarding gap investigated → filed [`ai-agent-harness#9`](https://github.com/itsginfo/ai-agent-harness/issues/9) (QUICKSTART.md refresh into onboarding doc + 3-surface MethodRX tracker drift + MONDAY.md doc-only deprecation); interim answer: point MethodRX at its self-loading `/Users/jamesmeirowsky/Projects/method-rx/CLAUDE.md`. (2) Graphify (`safishamsi/graphify`) grilled as wiki replacement → **rejected** (orthogonal layers: wiki = session-earned judgment, graphify = machine-derived structure; no felt pain); parked as candidate Phase C query-op via 2 comments on [`ai-agent-harness#3`](https://github.com/itsginfo/ai-agent-harness/issues/3) (incl. unverified ~70x token-claim addendum + rate-limit-headroom relevance). No ADR / verdict / CONTEXT.md change (fails 3-of-3). Default Claude Code model switched to Fable 5 (user-level config). |
| 2026-06-04 | PM Agent | **Tandem booking calendar weekday-header row restored ([`#12`](https://github.com/itsginfo/skydivecity-com/issues/12), routine request, closed).** Client reported Su Mo Tu … labels missing on the Burble PICK DATE step. Root cause was our own CSS: the `#7` empty-date-row collapse rule (`tr:not(:has(td:not(:empty)))`) matched the jQuery-UI `<thead>` header row (holds `<th>`, no `<td>`) and hid it. Fix: scope to `tbody tr`. `tandem-bookings-page-styles.css` committed for the first time (`90148b8`, `develop`); Burble CSS panel updated + verified live by James. Wiki [`burble-integration.md`](../../projects/skydivecity/wiki/burble-integration.md) updated (defect + session + tracked-file note). |
| 2026-06-02 | PM Agent | **May Digital Ops Monthly Report DELIVERED — `#10` closed.** James sent the report (PDF `2026-05.pdf`) To Rich, CC Matt on 2026-06-02 — 3 days ahead of the SOW §4.1 deadline (2026-06-05). First monthly Managed Services report complete. Prior session's delivery email draft was never persisted (conversation-only) → reconstructed faithfully from `2026-05.md` this session. `#10` body checkboxes completed + closed with delivery comment. Live Watch: May-report row retired, June-report row (due ~2026-07-05) added. Lingering Beyond Marketing conversion-counter verification remains tracked on `#9`. |
| 2026-06-01 (later) | PM Agent | **May Digital Ops Monthly Report content-complete + rendered (`#10`).** All 6 §§ filled: §1 100% uptime (UptimeRobot API + new `pull-uptime.sh`), §2 404 (live WP-CLI; ~7d retention caveat; scanner-noise vs legacy URLs), §3 GSC 376 indexed + decline-trend flagged for monitoring (`#11` GSC set up: verification file at `/www/`, see [`flywheel.md`](../../projects/skydivecity/wiki/flywheel.md)), §4 GA4 liveness (`purchase` ×166 / $68,298, partial-month) reconciled vs Rich's 538 Burble reservations (window+unit mismatch → directionally consistent, no false variance). PDF rendered (md-to-pdf, 5pp). Commits `f8b8243`→`d1e0e07`. **Awaiting James review + send** (To Rich, CC Matt). GA4 SA-key automation blocked by org policy `iam.disableServiceAccountKeyCreation` (correct default); OAuth path optional pre-June. |
| 2026-06-01 | PM Agent | **SSL cert renewal verified resolved — `#4` closed.** Auto-renewal fired May 8; live cert is Google Trust Services wildcard (`skydivecity.com` + `*.skydivecity.com`), valid → 2026-08-06; next renewal ~2026-07-07, no action. Corrected issuer fact in [`wiki/flywheel.md`](../../projects/skydivecity/wiki/flywheel.md) (was wrongly "Let's Encrypt"). Beyond Marketing visibility checkbox dropped per James. SSL row retired from Live Watch. |
| 2026-05-28 (later) | PM Agent | **E-commerce Merch Discovery grilling (Project Work) — Q5 complete + Burble-tender finding.** Square-everywhere single-ledger locked (Q1–Q4); catalog shape/scale + no-personalization + staff-owned photos/copy (Q5); flagged "no catalog owner today" risk. ⛔ Inventory branch blocked — customers can pay for merch with Burble balance → likely 3rd stock-decrementing tender channel, gates the single-ledger architecture (candidate ADR). Commit `1e304ac`. See [`ecommerce-merch-discovery.md`](https://github.com/itsginfo/skydivecity-com/blob/develop/project_management/ecommerce-merch-discovery.md). |
| 2026-05-28 | PM Agent | **Digital Ops Monthly Report system built (SOW §4.1).** Reusable template + procedure README + May 2026 first-report draft (§5/§6 filled, §1–§4 pending post-month-end pulls); *Reportable Work* principle added to `CONTEXT.md`. Commit `072e6af`. May delivery tracked at [`skydivecity-com#10`](https://github.com/itsginfo/skydivecity-com/issues/10) (deadline 2026-06-05). |
| 2026-05-20 | CTO Agent | **Tool-landscape v1 COMPLETE + harness #7 done.** All 10 verdicts (V-001 → V-010) + 8 ADRs + crib block + per-project propagation + 4 stretch artifacts (Matrix / Conflict log / Workflow guide / 3 Mermaid views) landed; harness `CLAUDE.md` v1.5 → v1.6 + both slash command files (`session-start.md` + `session-end.md`) flipped tracker-agnostic + V-003-aware. Commits: `1847e12` (Pass A) → `cbc4282` (Pass B harness) → `3ec4df9` (Pass B SkydiveCity) → `bd260e3` (stretch artifacts) → `9567cb7` (Mermaid parse-error fix) → `20ec9af` (session-end PROJECT_STATE drain) → `4298e4d` (#7 close). Closed: [`#8`](https://github.com/itsginfo/ai-agent-harness/issues/8) (tool-landscape v1) + [`#7`](https://github.com/itsginfo/ai-agent-harness/issues/7) (harness Monday-first cleanup). See [`TOOL_LANDSCAPE.md`](../../TOOL_LANDSCAPE.md). |
| 2026-05-20 | CTO Agent | **Tool-landscape v1 Session 2 closed (V-010 landed, 7/7 in the book).** `/status` is read view; `PROJECT_STATE.md` is write surface (V-003). Both `/status` copies rewritten in-verdict to drop Monday MCP refs. No ADR (fails 3-of-3 test). [`TOOL_LANDSCAPE.md`](../../TOOL_LANDSCAPE.md) updated. |
| 2026-05-20 | CTO Agent | **Tool-landscape v1 Session 2 (V-009).** Lifetime-axis split for recurring task surface. ADR [`0008`](../../docs/adr/0008-recurring-task-surface.md). |
| 2026-05-19 (later) | CTO Agent | **Tool-landscape v1 Session 2 — 5 of 7 verdicts (V-004/V-005/V-006/V-008/V-007).** ADRs [`0005`](../../docs/adr/0005-review-pipeline-sequencing.md), [`0006`](../../docs/adr/0006-review-retro-boundary.md), [`0007`](../../docs/adr/0007-intake-pipeline-sequencing.md). Closes [`ai-agent-harness#4`](https://github.com/itsginfo/ai-agent-harness/issues/4). Commits `b00f6f0` `e551c47` `f1ed0da` `2191369` `e476b0a`. |
| 2026-05-19 | CTO Agent | **Tool-landscape v1 Session 1 continuation (V-002 + V-003).** ADRs [`0003`](../../docs/adr/0003-boot-context-split.md), [`0004`](../../docs/adr/0004-project-state-shape.md). Knowledge-surfaces summary table added to TOOL_LANDSCAPE.md; V-NNN renumbered. *(First V-003-compliant Session Log row.)* |
| 2026-05-18 (late-late) | PM Agent | **Tool-landscape v1 Session 1 partial — grilling + scaffold + V-001.** `/grill-with-docs` on [`ai-agent-harness#8`](https://github.com/itsginfo/ai-agent-harness/issues/8); harness `docs/adr/` established. ADRs [`0001`](../../docs/adr/0001-tool-landscape-establishment.md) + [`0002`](../../docs/adr/0002-adr-vs-decisions-md.md). |
| 2026-05-18 (late) | PM Agent | **Wiki Phase B live + Quick-Index embedded + tool-landscape reconciliation filed.** 6 entity pages in `projects/skydivecity/wiki/`. Closes [`#2`](https://github.com/itsginfo/ai-agent-harness/issues/2); [`#3`](https://github.com/itsginfo/ai-agent-harness/issues/3) deferred-tracker (re-eval ~2026-08-18); filed [`#8`](https://github.com/itsginfo/ai-agent-harness/issues/8). Commits `2b1e4ff` `ccbc752`. |
| 2026-05-18 (mid-day) | PM Agent | **Monday.com subscription cancelled — `ai-agent-harness#6` Step 7 complete, migration fully closed.** 11-day soak ended clean. Archive at `skydivecity-com/project_management/monday-archive/`. |
| 2026-05-18 (mid-day) | PM Agent | **Burble GTM container ID whitespace fix — booking-funnel analytics restored.** 3 leading spaces in admin form → GTM never loaded for ≥12 days. Filed + closed retroactively at [`skydivecity-com#9`](https://github.com/itsginfo/skydivecity-com/issues/9). Test checkout confirmed full funnel firing in GA4 Realtime; 2 verifications carried to Live Watch. |
| 2026-05-17 (late) | CMO Agent | **Homepage Tandem Skydiving Rates updated.** $249 / $199 / Pasco County Resident Special. Filed + closed [`skydivecity-com#8`](https://github.com/itsginfo/skydivecity-com/issues/8). Inventory `migration/wp-homepage-rates-inventory.php` (`7a512dc`); update `migration/wp-homepage-rates-update-2026-05-17.php` (`94b0b95`). Lesson reinforced: dev-first applies to inventory too. |
| 2026-05-17 (earlier) | CMO/PM Agent | **`#7` post-launch follow-up.** One customer report on Burble step-2 buttons; DOM diagnostics confirmed CSS correct, attributed to client-side environment. Watch criteria: 2nd report opens new issue. |
| 2026-05-16 | CMO Agent | **Burble booking page — Back/Next visibility gated + Next disabled until slot selected.** Filed + closed retroactively at [`skydivecity-com#7`](https://github.com/itsginfo/skydivecity-com/issues/7). |
| 2026-05-14 | PM Agent | **🎉 SOW v1.1 + MNDA v1.0 EXECUTED — paper trail closed.** Both countersigned 2026-05-12 (reported by James 2026-05-14). Engagement transitions to active Managed Services footing. DECISIONS.md 2026-05-12 entry. |
| 2026-05-13 | CMO Agent | **Booking-page deposit/reschedule policy live on Burble.** Filed + closed [`skydivecity-com#6`](https://github.com/itsginfo/skydivecity-com/issues/6). |
| 2026-05-11 (eve) | PM Agent | **3 new 2027-season events live on prod.** WINTERFEST/FLOCKFEST/FLAMINGOFEST. `migration/wp-events-add-2027.php` (`214b49e`). 5-phase change-control procedure established as standing pattern (DECISIONS.md 2026-05-11). [`skydivecity-com#5`](https://github.com/itsginfo/skydivecity-com/issues/5). |
| 2026-05-09 (AM) | PM Agent | **SOW v1.1 issued; MNDA v1.0 drafted then held back.** Both sent separately to Rich (To) / Matt (CC) later that day per James's two-beat sequencing. DECISIONS.md 2026-05-09 entry. |
| 2026-05-07 (PM, eve) | PM Agent | **Migration Session β — Monday → GH cutover landed (Steps 4–5).** Boot-sequence flip across 4 files + symmetric SESSION_END flip + Per-Project Overrides table additions. Commits `2f5b935` (skydivecity-com), `1366317` (harness). Follow-up filed at [`ai-agent-harness#7`](https://github.com/itsginfo/ai-agent-harness/issues/7). 14-day soak begins. |
| 2026-05-07 (PM, mid-late) | PM Agent | **Migration Session α — Monday → GH (Steps 0–3.5).** ADR-0001 + ADR-0002 (skydivecity-com `f0656dc` `b809f84`); 89 items archived (`b0a4ba4`); 9 open items ported as GH issues; GH Project #1 + 10 labels created; migration tracker [`ai-agent-harness#6`](https://github.com/itsginfo/ai-agent-harness/issues/6). |
| 2026-05-07 (AM) | PM Agent | **Managed Services SOW v1.0 delivered to Rich.** PDF generated; sent To Rich / CC Matt. DECISIONS.md 2026-05-07 entry. |
| 2026-05-06 | PM Agent | **Managed Services SOW v1.0-DRAFT via `/grill-with-docs`.** Six grilling outcomes; `CONTEXT.md` created at repo root. DECISIONS.md 2026-05-06 entry. |
| 2026-05-05 (eve) | CTO Agent | **Matt Pocock per-repo skill config + GitHub Issues adopted.** `docs/agents/{issue-tracker,triage-labels,domain}.md` scaffolded; `CLAUDE.md` `## Agent skills` block added. Commit `fc5990f`. |
| 2026-05-05 | PM Agent | **🎉 Phase 1 ACCEPTED.** Rich Muscolino written acceptance. Completion Report (technical + executive PDF) drafted facts-only. R-1 through R-6 closed Done. |
| 2026-05-04 | PM Agent | **Phase 1 monitoring window closed (100.000% / 168h); acceptance request sent.** SOW-1 (2-prong) + SCOPE-1 (Burble) scope captured from James. |
| 2026-05-02 (mid-AM) | CTO Agent | **Harness-level `wiki/` created.** HARN-* design docs relocated from project wiki to harness wiki via `git mv`. |
| 2026-04-30 (PM) → 2026-05-02 | PM Agent | **Days 3-5 monitoring (manual).** 100% / 24h × 3 days. Day 6 (May 3) skipped (Sunday). |
| 2026-05-01 (eve) | CTO Agent | **HARN-6 abandoned; multi-team branching reframe adopted; HARN-5 trial decisively successful.** 8 adversarial-review passes / 14 findings on single-tree solutions failed to converge. TBD-with-flags rejected. Commit `00fb926`. |
| 2026-04-30 (late eve) | CTO Agent | **HARN-5 pull-forward + Codex plugin activated.** ChatGPT-account auth; reviewGateEnabled false. Trial scope: adversarial-review on non-PHI PRs. |
| 2026-04-30 (eve) | CTO+PM Agent | **Phase 2 redesign discovery branch (`feature/redesign-phase2`) cut local-only; HARN-6 surfaced.** Multi-domain audit strategy discussed; paused before HARN-6 strategy choice. |
| 2026-04-30 (PM) | PM Agent | **HARN-2 Phase A executed.** `projects/skydivecity/wiki/` created with `README.md` + `sources.md`; SESSION_END Step 5c (Wiki ingest) added. |
| 2026-04-29 | PM Agent | **Day 2 monitoring; daily-checkin routine DISABLED.** Sandbox allowlist blocks UptimeRobot API + skydivecity.com fallback curl. Lesson captured at [`wiki/sandbox-allowlist.md`](../../projects/skydivecity/wiki/sandbox-allowlist.md). |
| 2026-04-28 | PM Agent / CTO Agent | **QA execution + W4-19/W4-20 retraction + daily-checkin routine patched (later disabled).** Final QA verdict YELLOW with defect list. |
| 2026-04-27 (eve) | PM Agent | **Post-cutover ACF discovery + fix.** 3-of-3 new pages rendered empty; `mywp` is fully ACF-driven. `migration/wp-page-acf-import.php` authored; QA addendum filed. Pattern documented at [`wiki/wp-acf-rendering.md`](../../projects/skydivecity/wiki/wp-acf-rendering.md). |
| 2026-04-27 (PM) | PM Agent | **Retro execution + closeout.** First retrospective: `projects/skydivecity/retrospectives/2026-04-27.md` (`2d1b9ec`). Pattern P-002 ("harness under-leveraged") registered. |
| 2026-04-27 (AM) | PM Agent | **🎉 Phase 1 Cutover complete.** 44/46 events imported, 3 pages created, 3 nav redirects, 9 CTAs, 8 featured images, 781 image files rsynced (107 MB). Release-success email sent. Commits `720e95d` `ee43aaf`. |
| 2026-04-26 | PM Agent | Pre-flight session for Apr 27 cutover; GA4 baseline captured; Flywheel SSH outage W4-13 escalated. |
| 2026-04-24 | CTO/CMO/PM Agent | Cutover GO confirmed (April 27, 9 AM ET); CMO-2 DESIGN.md rebuilt via Stitch; workspace reconciliation. |
| 2026-04-23 | CTO/PM Agent | Closed W1-10/W3-7/W4-3/W4-10. Rich → Primary SPOC. Initial git commits + GitHub remote sync. |
| 2026-04-22 | PM Agent | Harness integration — read Monday board (89 items), reconciled state, created PROJECT_STATE.md. |

---

## Recovery Checkpoints

*(None)*
